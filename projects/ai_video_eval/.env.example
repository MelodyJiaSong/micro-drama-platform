# Only needed for judge.engine: api (Anthropic API direct).
# The default engine (claude_code) runs on your Claude subscription and needs no key.
# The eval system reads ONLY this file + config/eval_config.yaml —
# never .claude/, CLAUDE.md, or repo-level settings.
AI_VIDEO_EVAL_ANTHROPIC_KEY=sk-ant-...
