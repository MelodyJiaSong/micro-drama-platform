# hy4 follow-ups · 2026-09

---

## 001 — 2026-09-13 18:10:00 — turntable 是静帧四视图，缺 4s 建立视频 prompt

> target_stage: 2
> target_artifacts:
>   - ai_videos/huangye_shenghuo/hy4/2_世界观人设/characters/c1_凿冰的女人/c1_凿冰的女人.md
>   - ai_videos/huangye_shenghuo/hy4/2_世界观人设/casting.md
> severity: medium

### 指令

hy4 没有人物 4 秒建立视频的 prompt，为什么缺？

### 诊断（为什么缺）

**不是漏写了一段，是整条 turntable 走了错的形态。** hy4 的 `c1-2` 被做成
**四视图静帧图**（`c1-2_凿冰的女人转身.png`），而 `casting.md` 的清单行与 18 个 shot 的
Reference-uploads 全都按 PNG 写 —— 内部完全自洽，所以没有任何地方看起来"缺了东西"。

而 `格式契约` K16b 要求：每张**命名人形角色卡**必须带 **4s 建立视频**块
（`时长: 4s`、5-phase `0-1/1-1.5/1.5-2.5/2.5-3/3-4`、抽帧 front 0.5 / side 2.0 / back 3.5），
**静帧转面图不是合格形态**。

三层原因，从近到远：

1. **hy3 原来也是这个毛病**，是跑 `格式契约` 时被 K16b 抓到才改成 4s 视频的。
   而当时给 skill 的范围是 `ai_videos/huangye_shenghuo/hy3` —— K16b 的「生效范围」条款
   明确规定**只在被显式指定的范围内报违规**。hy4 从没进过 scope，同一个毛病就没人报。
2. **hy1 有正确的块、hy2 靠 `.link.json` 复用 hy1 的人**，所以系列内唯一能照抄的
   正确样板只有 hy1 那一份，而 hy3/hy4 都是"每部换人"的新建卡，都得从头写一遍。
3. **根本原因：人物卡与道具卡没有生成器。** 镜头有 `gen_shots_hy4.py`、场景有
   `gen_scene_prompts_hy4.py`，它们的契约由 build-time gate 守着（`shot_logic.gate` /
   `prompt_light.gate`，一犯直接 raise、分镜根本生成不出来）。人物/道具卡是每部**手写**的，
   于是「turntable 必须是 4s 视频」这类契约每部都要重新记一遍。这正是 rule 4i ① 要防的
   那类失效 —— **没有单一出处的东西，一定会漂**。

### 落地

- c1 卡的 turntable 段整段重写为 **4s 建立视频**块（照 hy1/hy3 的结构，内容用 hy4 自己的
  服装与装备）：16:9 · 4s · 5-phase 时间轴 · 抽帧点 · 验收五条 · `参考: c1-1.png(脸的唯一标准)=>@`。
  - **发型写成反向声明** —— hy4 全卡从未声明发型（毛线帽全遮），所以写
    `毛线帽全遮，无外露头发` + 负向 `露出头发 / 帽子脱下`，**不替它编一个发型**。
  - **turntable 不背包** —— 背包只属 shot01–03 的移动镜（rule 16 ②），
    而背面视图要交代的恰好是外套后身，背上包全遮住了。
  - **手取 shot01 的干净态**（手的编年史起点），负向挂 `手部裂口污迹`。
- 顺带修同源的第二处：**落盘文件名声明仍带中文视图名**（`c1-1_凿冰的女人锚点.png` 等）。
  导入器现在按路由键落盘（裸 `c1-1.png`），卡上的声明会与盘上实际不符。
  c1/c2 卡 + 5 张道具卡 + `casting.md` 全部改为裸键；`tools/gen_shots_hy4.py` 的
  Reference-uploads 路径同步改后**重跑生成器**（不手改产物，rule 4i ①）。

### 一行摘要

hy4 的 c1-2 被做成静帧四视图而非 K16b 要求的 4s 建立视频；根因是人物卡无生成器、契约靠每部手写，
且 K16b 上次只在 hy3 的 scope 里跑过。已补 4s 视频块，并顺带统一落盘裸键命名。

---

## 002 — 2026-09-13 19:10:17 — 雪铲凭空出现 · shot01→02 接缝突兀 · shot02 撞树 · 雪井太小

> target_stage: 6
> target_artifacts:
>   - tools/gen_shots_hy4.py
>   - tools/gen_scene_prompts_hy4.py
>   - ai_videos/huangye_shenghuo/hy4/2_世界观人设/scenes/shenxue/bg3_雪井原始/bg3_雪井原始.md
> severity: medium

### 指令（实拍反馈，shot01–04）

1. **shot01 / shot03 / shot04**：背包上看不出带着雪铲，到 shot04 雪铲突然冒出来，突兀。
2. **shot01 结尾那只手 → shot02** 衔接突兀。
3. **shot02** 身体斜着撞树的动作很傻，去掉，或者改成用手拍一拍。
4. **shot03 / shot04**：背景图里的树坑太小，根本挖不出洞；人跳进去以后坑又突然变大。
   是背景图的问题，要重新出图，**坑要大**。

### 诊断

1. prompt 只写「背着背包」，shot04 才写「从包侧抽出雪铲」——**雪铲在取出之前一次都没被描述过**，
   前三镜也没上传 `p1-1`，模型只能画一个空包。
2. shot01 以手部特写收尾，**手的动作没有结果**（按一下雪，然后呢？），下一镜人已经在林子里——
   **因果断了**，所以才显得突兀；景别切口本身（4.89）没有问题。
3. 「从低枝下侧身穿过、肩膀蹭到枝条」被模型演成人斜着身子撞上树。
4. bg3 写的是「直径约三米、低约一米二」，模型画成浅碗；机位又是贴地平视、略俯十度，
   更看不出深度。而 shot05–08 要在坑底向下掏到地面、再横掏出能躺人的洞，模型只好当场放大坑。

### 落地

- **雪铲挂法锁成一条串**（`CARRY`）：整把雪铲竖着扣在包背面，D 形握把高出包顶一拃、背着时从右肩后露出来；
  雪锯插在右侧外袋。shot01–03 的装备状态、`走位:` 原样带上，`p1` 加入道具与参考上传；
  shot03 卸包时雪铲跟着包下来；shot04 改成「**从包背面解下扣着的雪铲**」。
  顺带修正：shot04 的装备状态原先写「本镜内卸包」，实际是 shot03 卸的，新增 `坑沿` 状态。
- **shot01 重排（仍 26s）**：手部特写挪到中段，**手插进雪里一拃深就顶到冰——湖上雪太薄**；
  切回同一远景，她戴回手套、**转身走向对岸林墙**。末帧改为远景背影（0.10），
  接 shot02 全景 0.45 比值 0.22 ✅，两端机位不同 ✅。shot18 的「与 shot01 末段同机位」改为「手部特写段」。
- **shot02**：改成抬手拨开挡路的低枝、拍两下，正着身子走过去、身体不碰树；「擦过帽顶」改成「帽顶上方一拃处经过」；
  负向加撞树抑制词组（`NEG_BUMP`）。
- **bg3 雪井放大**：直径约六米（树干五倍）、深约一米八（比人还深）、远侧坑壁高过一人、树干下半截露在坑里；
  **N1 机位抬到一人高、俯角约二十五度**，才看得出深度；验收第一条改成尺寸，负向加浅坑词组。
  bg8 同步机位与尺寸；shot03/04 场景串、`world.md`、`script.md` 同步。
- **规则进化**：`ai_video.md` 16.13（工具取出前每镜看得见）· 16.14（锚点尺寸按施工倒推 + 人体尺度参照）·
  16.15（环境反应用手触发，不用身体撞）。

### 一行摘要

雪铲取出前从没被描述、shot01 的手没有结果、侧身穿枝被演成撞树、雪井写得太小又贴地平视——
已锁雪铲挂法、shot01 改成「手试出雪太薄 → 转身走向林子」、shot02 改用手拨枝、bg3 放大到六米×一米八并抬高 N1；
**bg3/bg4/bg8/bg11 需重出图**。

---

## 003 — 2026-09-13 19:40:00 — bg3 雪井仍不够大：再放大，深度翻倍

> target_stage: 2
> target_artifacts:
>   - tools/gen_scene_prompts_hy4.py
>   - ai_videos/huangye_shenghuo/hy4/2_世界观人设/scenes/shenxue/bg3_雪井原始/bg3_雪井原始.md
> severity: low

### 指令

bg3-1 里的树坑还是不够大，至少再大一些，**深度翻倍**。

### 落地

- 雪井：直径 6m → **约 10m（树干八倍）**，深 1.8m → **约 3.6m（两人高，「两个成年人叠起来才够得着坑沿」）**，
  下半段坑壁近乎竖直像井；树冠雪影 9m → 14m。
- **补一个让尺寸成立的设定**：这棵树长在背风洼地，四周风积雪五米多深（林子别处仍是两三米）——
  否则 3.6m 深的坑已经到底，shot05 往下再掏 1.2m 竖井到地面就不成立。
- N1 俯角 25° → **35°**，看得见坑底；验收加「宁可画得夸张也不许画小」，负向加 `坑深不过一人 / 坑底一眼就到`。
- 同步：bg8 尺寸与机位；shot03 场景串/情节/机位；shot04 滑下「三米多」、坑沿比她帽顶还高一人；`script.md`、`world.md`。
- 规则：`ai_video.md` 16.14 补第⑤条——模型系统性往小里画，锚点尺寸按「一眼夸张」写。

### 一行摘要

bg3 雪井放大到直径十米、深三米六（两人高），以背风洼地五米风积雪自洽，N1 俯角加到 35°；bg3/bg4/bg8/bg11 仍需重出图。

---

## 004 — 2026-09-13 19:55:00 — bg3 雪井深度够了，宽度不够

> target_stage: 2
> target_artifacts:
>   - tools/gen_scene_prompts_hy4.py
>   - ai_videos/huangye_shenghuo/hy4/2_世界观人设/scenes/shenxue/bg3_雪井原始/bg3_雪井原始.md
> severity: low

### 指令

bg3 的树坑深度差不多了，但是不够宽。

### 诊断

003 为了写深度加了「下半段坑壁陡得近乎竖直，**像一口大井**」——「井」本身就是窄的意象，
模型把坑往窄里画；而「直径约十米」光写米数，模型照例不认。

### 落地

- 深度保持约 3.6m；直径 10m → **约 16m（树干十三倍，宽是深的四倍多）**，树冠雪影 14m → 20m。
- 删掉「像一口大井」，改成「**又宽又深的大漏斗，不是一口窄井**」「下半段才变陡」。
- **宽度写成画面占比**：「坑口左右两侧的坑沿都伸出画面之外，画面下半部分几乎全是坑」；
  验收同步，负向加 `窄井 / 竖井状的窄坑 / 坑口只占画面一小块 / 坑沿完整收在画面里`。
- **N1 焦段 28mm → 20mm 广角**（shot03 / shot09 / shot17 同步，保持三次对比帧同焦段）。
- 同步：bg8 尺寸；shot03 场景串与情节；`script.md`、`world.md`。
- 规则：`ai_video.md` 16.14 ⑤ 补「宽度别用窄比喻，写画面占比 + 宽深比，必要时换广角」。

### 一行摘要

bg3 雪井加宽到直径约十六米（深仍三米六），去掉「像井」的窄比喻、改写成坑沿两侧出画，N1 换 20mm 广角。

---

## 005 — 2026-09-13 20:05:00 — bg3 雪井直径再大一倍

> target_stage: 2
> target_artifacts:
>   - tools/gen_scene_prompts_hy4.py
> severity: low

### 指令

bg3 雪井的直径要再大一倍。

### 落地

- 直径 16m → **约 32m（树干二十多倍，宽是深的九倍）**，深度仍约 3.6m；写「远侧坑沿远在树后很远处」。
- 单棵树冠挡雪解释不了 32m 的坑，**补成因**：树冠挡住中心、风绕着孤树打旋把四周的雪一圈圈掏走 ——
  「雪井」改称「雪窝」；删掉「树冠雪影二十米」，改「树冠罩住坑中央一大片」。
- 负向加 `坑宽不过几棵树 / 小雪窝`；bg8、shot03 场景串与情节、`script.md`、`world.md` 同步。
- 规则：`ai_video.md` 16.14 ⑤ 补「尺寸以用户看图认可为准，物理说不通时补成因」。

### 一行摘要

bg3 雪窝直径加到约三十二米（深三米六不变），补「风绕孤树打旋掏雪」成因使画面自洽。

---

## 006 — 2026-09-13 20:20:00 — bg3 雪窝再深再大：用「能盖下一间小屋」给尺度

> target_stage: 2
> target_artifacts:
>   - tools/gen_scene_prompts_hy4.py
> severity: low

### 指令

坑要再深一点、再大一点。光写数字可能不够，要直接告诉模型：**深度和宽度要能在里面盖一间小屋，人能站在里面**。

### 落地

- 深 3.6m → **约 4.5m**，直径 32m → **约 40m**；风积雪五米多 → **六米多**（shot05 竖井再下 1.2m 到地面仍成立）。
- **用功能容量写尺度**：「整间小屋放进坑底，屋顶都不会冒出坑沿」「一个成年人站在坑底，头顶离坑沿还差两个多人高」
  「坑底是一大片平坦开阔的雪地，大得能盖起一间带门的小屋、四周还能绕着走一圈」「远侧坑壁比一间小屋还高」。
- **坑底改成平坦开阔**（原先漏斗形隐含尖底）。
- **参照物反向声明**：「小屋只是尺度参照——画面里没有任何屋子」，负向加 `坑里有屋子 / 坑底窄得站不下几个人 / 坑底是尖的`
  （负向原本就有 `木屋 / 帐篷`）。
- 同步：bg8；shot03 场景串/情节/看点；shot04 滑下「四米多」、坑沿比她帽顶高两个多人；`script.md`、`world.md`。
- 规则：`ai_video.md` 16.14 ⑤ 补「数字压不住时写能装下什么，参照物不入画要反向声明」。

### 一行摘要

bg3 雪窝加到深约四米五、直径约四十米、坑底平坦，用「能盖下一间小屋、人站坑底离坑沿还差两个多人」给尺度并声明画面里无屋。
