# changelog — hy2（荒野生活 · 红杉第一夜）

## 立项 → 出 prompt 一轮到底 — 2026-09-09
Source: `user_input/raw_prompt.md` + 同日 AskUserQuestion 四项裁定
Summary: 新建《荒野生活》系列第 2 部，从立项一路做到 12 镜标准化 prompt。

**四项裁定**：真嵌套目录（改 webapp）· 系列统一改叫《荒野生活》· 加州海岸红杉 · 空心红杉树洞屋（goosepen）。

新增（阶段 1–6 一轮到底）:
- `1_立项/concept.md` — 立项策划单：为什么是这个题材（hy1 是「造」，hy2 是「住进去」）、
  世界设定、10 条爽点、只给一次的两样（香蕉蛞蝓 / 内壁旧刻痕）、段落结构、与 hy1 的四格差异核对、明确不做。
- `2_世界观人设/world.md` — 11 个 bg 主体清单 + 改造状态链 + **8 条真实性铁律**（含「树是活的」「火离壁一臂」「不砍活树」）
  + 一天时序 + 三次同机位对比帧（N1）+ 无声叙事约定。
- `2_世界观人设/style_guide.md` — 冷白灰 vs 深红棕的调色、四段光、**两个奇观构图**、
  负向锁定块（含红杉篇专属与夜镜安全词）、露脸分布、音轨约定（空旷感靠长混响）。
- `2_世界观人设/{relationships.md, casting.md}` — 人物网（人 ↔ 树 ↔ 风暴 ↔ 蛞蝓 ↔ 刻痕，**树是盟友**是系列首次）；
  选角 N/A + 声音设计表。
- `2_世界观人设/characters/{c1_造家的人, c2_香蕉蛞蝓}/` — c1 跨片复用只写状态差异（脏污线：黑炭→灰→红棕屑）；
  c2 只出现一次、非人形（K28 豁免，同 hy1 犀鸟）。
- `2_世界观人设/props/{p1_砍刀, p2_随身装备, p3_树皮门板}/` — p1/p2 跨片复用；
  **p3 树皮门板是本片新增核心道具**，含四阶段成型时序与锁定描述符。
- `2_世界观人设/scenes/hongshan/bg1..bg11/` — 11 主体 / 18 视图，由 `tools/gen_scene_prompts_hy2.py` 生成。
- `3_大纲/outline.md` — 段落总表 + 12 镜切分表 + **三个原理各用一整段说清楚** + 露脸分布 + 三条实拍纪律 + 14 项贯穿检查表 + 声音结构。
- `4_剧本/script.md` — 画面散文本（无台词 → 无 dialogue.md）。
- `5_6_分镜与prompt/{shotlist.md, shots/shot01..shot12/, all_shot_prompts.md, publish.md}` —
  12 镜 / 278s，由 `tools/gen_shots_hy2.py` 生成。
- `README.md` — 项目概要 + 与 hy1 的四格差异 + 目录结构 + **18 张图的出图顺序与三条铁律**。
- `specs/ai_video/hy2/{raw_prompt.md, divergence.md, changelog.md}`。

生成器:
- `tools/gen_scene_prompts_hy2.py` — 11 主体 / 18 视图。**视图集按 rule 4d④ 收敛**（hy1 是 58 张，本片 18 张）。
- `tools/gen_shots_hy2.py` — 12 镜。字段：`bag`（不背包）/ `fall`（真实重力）/ `door`（门板四阶段）/
  `cuts`（镜内分段）/ `vs`（多场景参考）；有火的镜自动追加**用火安全负向**。

QC（机检）:
- 字段齐全（13 个必填字段）· 零 hex · K27 裸 `=>@` · 比例 16:9 · 时长全在 15–30s ·
  无字幕泄漏 · prompt 块最长 **2554 字**（上限 5000）—— **全部通过**。
- 参考图 19 张全部待出（含 c2-1），符合预期。
- `c1` / `p1` / `p2` 指向 hy1 的既有资产，不重出图。

结构:
- 本片是第一个落在**系列嵌套结构**上的项目（`ai_videos/huangye_shenghuo/hy2/`）；
  webapp 侧的支撑改动见 `specs/development/ai_video_management/` follow-up 165。

## Follow-up 001 — 2026-09-09 15:00:00
Source: user_input/follow_ups/202609.md - section 001
Summary: 住所形态由「空心红杉树洞」换成「倒木根盘下的地窝」，原理由三个收敛到一个「能烧的暖床」。阶段 1–6 全部重做。

调研（新增）:
- `1_立项/shelter_research.md` — 用 `tools/yt_research.py`（yt-dlp 实测）跑 8 组搜索视角、
  83 个去重候选，对 22 条相关的跑 `meta` 取真实赞率与订阅数。三条读数：
  ① 树洞确实是同类形态里房间最小的一档（用户判断成立）；
  ② 播放最高的两条（296M / 280M）赞率垫底或中游，泳池隧道那一派该降档；
  ③ 赞率前六里五条是「外面极端 + 里面是暖的」。
  另如实记录一条落差：实测赢家几乎全是 15–40 分钟长片，本片做四分半，**短版必须只留一个原理**。

用户裁定（AskUserQuestion）: 倒木根盘下的地窝 · 只留「能烧的暖床」这一个原理。

重做（阶段 1–6）:
- `1_立项/concept.md` — 全文重写：为什么换形态（含调研四条理由）、唯一的原理四步、
  世界设定、10 条爽点、只给一次的两样、段落结构、与 hy1 的四格差异、明确不做。
- `2_世界观人设/world.md` — 全文重写：12 个 bg 主体、**那棵倒木的物理前提**（根盘 4m / 坑的尺寸 / 树是死的）、
  8 条真实性铁律（含「烟道必须走床下并通到根盘背面」「床是热的靠两个可拍现象证明」）、
  时序、三次同机位对比帧（N1 改为坑口正前方）。
- `2_世界观人设/style_guide.md` — 全文重写：调色改为「冷白雾 + 深红棕 + 湿黑土 vs 床缝里的暖橙」；
  **光的骨架改为「火被藏起来」**（hy1 的火敞开照亮全屋，hy2 的火大半压在床下）；
  两个奇观构图（坑底仰拍 / 门缝同框）；负向新增**根盘专属**与**夜镜火的底线**。
- `2_世界观人设/relationships.md` — 人物网改写：**倒下的那棵树是盟友**（给了墙、坑、石板、树皮），
  根盘挡风是它唯一一次被证明。
- `2_世界观人设/casting.md` — 声音设计表改写，新增**火在床下所以火声是闷的**这条听感设计。
- `2_世界观人设/props/p3_树皮门与顶/` — 取代原 `p3_树皮门板`，含屋顶与门两条锁定串、四阶段时序。
- `2_世界观人设/scenes/hongshan/bg1..bg12/` — 12 主体 / 18 视图，全部重生成。
- `3_大纲/outline.md` · `4_剧本/script.md` · `5_6_分镜与prompt/*` — 全部重写/重生成。
- `README.md` · `publish.md` — 概要、四格差异、出图顺序、封面帧、章节时间轴、声音清单全部改写。

生成器:
- `tools/gen_scene_prompts_hy2.py` — 重写：12 主体 / 18 视图，状态链改为「往下挖」
  （原始坑 → 挖深垒边 → 烟道成型 → 床台盖好 → 建成 → 风暴前 → 风暴夜 → 远景）。
- `tools/gen_shots_hy2.py` — 重写：12 镜 / 283s。字段 `bag` / `fall` / `door`（四阶段）/
  **`bed`（床台成型即锁定，新增）** / `cuts` / `vs`；有火的镜自动追加**「火大半压在床下」的视觉底线负向**。

QC（机检）: 字段齐全 · 零 hex · K27 裸 `=>@` · 比例 16:9 · 时长全在 15–30s ·
无字幕泄漏 · prompt 块最长 **2679 字**（上限 5000）—— **全部通过**。

## Follow-up 002 — 2026-09-12 12:00:00
Source: user_input/follow_ups/202609.md - section 002
Summary: 人物/道具卡的出图 prompt 首行补上路由键；跨片复用的资产改挂 `*.link.json` 快捷链接（UI 可预览）。

成因: hy2 的 5 张资产卡是手写的，卡里的 ```text 块装的是**锁定描述符与负向清单**，
不是出图 prompt——首行是中文描述，导入按钮按 prompt 前几个字匹配，自然失配。
hy1 的卡是照 rule 4b 写的，5/5 都带键，没有这个问题。

Auto-updated:
- `2_世界观人设/props/p3_树皮门与顶/p3_树皮门与顶.md` — **新增出图 prompt `p3-1_树皮门板锚点`**
  （门板在 shot07–shot12 六镜反复上画，且内外两面长相完全不同，靠文字复述必漂 → rule 4b 要独立 ref 图）；
  锁定描述符移出 ```text，负向并进 prompt 的 `负面词:` 行。
- `2_世界观人设/characters/c2_香蕉蛞蝓/c2_香蕉蛞蝓.md` — 新增出图 prompt `c2-1_香蕉蛞蝓锚点`，同上整改。
- `2_世界观人设/{props/p1_砍刀, props/p2_随身装备, characters/c1_造家的人}/*.md` — 改写为**复用卡**：
  不写出图 prompt，只留锁定描述符（裸围栏）+ 本片用法差异 + 指向快捷链接。
- **新增 4 个快捷链接**（`*.link.json`）：c1 立绘 · c1 turntable · p1 砍刀 · p2 随身装备 → 全部指向 hy1 的原件。
- `tools/gen_shots_hy2.py` — 新增 `P3` 锁定串；`pr` 含 `"p3"` 的镜（shot07–shot12）在 `参考:` 行挂
  `p3-1(树皮门板锚点)=>@`、`道具:` 行带 P3；Reference uploads 里 c1/p1/p2 注明「本片目录下有快捷链接」。
- 已把用户先前生成的 `characters/c2_香蕉蛞蝓/c2.png` 改名为 `c2-1_香蕉蛞蝓锚点.png`（对齐新的路由键）。

上收为全局规则:
- `.claude/agent_refs/project/ai_video.md` — 新增 **rule 4b-A**（资产卡 prompt 首行必须带路由键；
  锁定描述符/负向不进可复制代码块）与 **rule 4b-B**（跨片复用走 `*.link.json`，不复制不软链）。
- `CLAUDE.md` § AI video rules — 对应一条。

参考图进度（2026-09-12 12:10 实测）: **20 个出图 prompt 里 13 张已在盘上**，待出 7 项——
`p3-1` 门板（本次新增）· `bg9-2` · `bg10-1` · `bg11-1` · `bg11-2` · `bg11-3` · `bg12-1`。

## Follow-up 003 — 2026-09-12 14:00:00
Source: user_input/follow_ups/202609.md - section 003
Summary: 镜间切口提为排镜第一约束；12 镜补 `景别档:`，修 4 处不合格接缝，审计做成生成器闸门。

审计结果（改前 11 个接缝）: **4 处不合格**——
- `shot04→shot05` 比值 0.67 弱，且两端**同是「坑口斜上方半俯视」**：铁律①的正脸。
- `shot08→shot09` 两端**同是 N1 对比帧机位**，逐帧同框、只有光在变。
  比值算出来 0.12 判 ✅ 是**假合格**——09 起幅没有人，`人占画高` 这把尺子量不到画框。
- `shot10→shot11` 两端**同是 N1 外景夜、同一个 plate**：全片最糟的一处。
- `shot11→shot12` 比值 1.21 弱，且同在坑内、方位只差 90°。
另有 `shot01→shot02`（1.50）与 `shot03→shot04`（1.54）踩在中档，一并升到强档。

Auto-updated:
- `tools/gen_shots_hy2.py` — **六处取景改动，不动镜数、不动时长、不动剧情顺序**：
  ① shot01 落幅由中景改为**走成近景**（机位不动，景别由他的脚走出来）；
  ② shot03 落幅坑底仰拍改为**他整个人退到画面下部、只占画高五分之一**（奇观也更成立）；
  ③ shot05 起幅由「坑口斜上方半俯视」改为**坑内贴地低角度近景**（火塘口与卵石）；
  ④ shot09 起幅由 N1 改为**剑蕨叶面微距**（雾横着流过叶面、水珠聚大坠落——
     雾反转在一片叶子上先讲一遍，比全景更清楚）；
  ⑤ shot11 起幅由 N1 外景改为**门板外侧微距**（雨顺纵沟成股流下、缝里一线橙光），
     5s 后再拉到 N1 全景，暴雨的建立一秒没少；
  ⑥ shot12 把**床面与石缝微距提到开场**（承上一镜「他侧头看火」的视线引导），
     手掌贴床与躺下并成一条连续机位，镜内少一次切。
  **六处都往「紧」的方向破，全部骑在已有 plate 上——不新增任何参考图。**
- `tools/gen_shots_hy2.py` — 新增 `jb=(起幅值, 起幅名, 落幅值, 落幅名)` /
  `jbcam=(起幅机位, 落幅机位)` 两个字段、`jb_verdict()` / `jb_audit()`，
  **build 时逐对算 11 个接缝，任一不合格直接 `raise`——不合格的分镜生成不出来**。
- `5_6_分镜与prompt/shots/*/shotNN.md` — 12 镜 `## Shot context` 新增 `景别档:` 行，
  含两端景别 + 人占画高 + 机位标签 + **与前一镜的比值和判定**。
- `5_6_分镜与prompt/shotlist.md` — `衔接` 列改为与上一镜的比值；末尾新增
  **「镜间切口审计」表**（11 行，含两端数值、比值、机位、判定）。

改后: **11 个接缝全部 ✅ 强（≥2.0 或 ≤0.5），且两端机位标签互不相同。**
比值依次 2.12 / 0.38 / 0.31 / 0.40 / 0.43 / 0.36 / 0.43 / 0.12 / 0.21 / 0.05 / 0.42。
时长仍为 **12 镜 283s**（未变），正向 prompt 最长 2912 字（K10 上限 5000）✔

上收为全局规则:
- `.claude/agent_refs/project/ai_video.md` — 新增 **16.5**（人占画高的洞：同机位同取景假合格 →
  机位标签不许相同）· **16.6**（切口第一、时长第二；合并/拆分是正当手段；4–30s 皆合法）·
  **16.7**（可以算的契约左移进生成器，不留给出片前的 reviewer）。
- `.claude/agent_refs/project/ai_video_jingbie.md` — §2.0.2-b（同上洞）· §2.5 · §2.6。
- `.claude/skills/ai_videos__格式契约/SKILL.md` — K31 新增判据 ⑥（同机位同取景＝blocker，不看比值）。
- `CLAUDE.md` — 【TOP PRIORITY】条补三句。

No conflicts found in: 参考图清单（六处改动全部骑在已有 plate 上，**待出图仍是原来那 7 张**）·
`ai_videos/huangye_shenghuo/hy2/2_世界观人设/`（未触及）

### 参考图进度更新 — 2026-09-12 15:55（补记 002/003 之后的实际状态）
用户在此期间又出了 6 张：`bg9-2` · `bg10-1` · `bg11-1` · `bg11-2` · `bg12-1`，以及 p3 门板。
p3 那张**落对了目录、却按目录名命名为 `p3_树皮门与顶.png`** —— 导入按钮匹配到了目录，
但文件名没跟上路由键，于是 shot07–09 的 `参考:` 行对不上。已改名为 `p3-1_树皮门板锚点.png`。
**教训补充给 rule 4b-A**：「prompt 首行带路由键」只保证**匹配到目录**，
不保证**落盘文件名**；出图导入后要顺手核一眼文件名是否为 `{键}-{N}_{视图名}`。
当前唯一缺口：**`bg11-3_床面与火口微距`**（prompt 在 `scenes/hongshan/bg11_地窝风暴夜/` 视图 3），
shot10 的主视图 + shot11/shot12 各一段微距挂着它。**shot01–shot09 参考图已齐，可开生成。**


## Follow-up 004 — 2026-09-12
Source: user_input/follow_ups/202609.md - section 004
Summary: publish.md 重构为四站发布页（YouTube/TikTok 英文 · 抖音/小红书 中文），一站一节、可直接复制。

### 改了什么
- **结构**：固定七节 —— ① 母版与通用设置 ② 封面帧 ③ YouTube ④ TikTok ⑤ 抖音 ⑥ 小红书 ⑦ 平台对照 + 发布前检查
- **新增 TikTok 节（英文）**：原先 TikTok 与抖音合成一节且只有中文——**TikTok 是英语区主阵地，中文文案等于白传**。现拆开，TikTok 用独立创作的英文 caption + 英文 hashtag
- **新增 小红书 节（中文）**：此前完全没有。带该站三条硬限——**标题 ≤20 字**（超出直接截断）、正文 ≤1000 字、话题 ≤10 个，并**单独出一张 3:4 竖裁封面**（横屏母版在小红书的封面很吃亏）
- **每块可复制内容单独进 ```text 围栏**（标题/描述/标签各一块，围栏里不夹说明）——平台输入框是分开的，合成一块就得手动拆
- **每节补齐非文案 metadata**：类别 / 语言 / 是否儿童内容 / AI 生成披露 / 播放列表或合集 / 许可 / 用哪一帧当封面 / 画幅与时长
- **画幅取舍写明**：三个竖屏站一律**横屏直投、接受黑边**（本季不出竖版），小红书封面例外做竖裁

Auto-updated:
- `5_6_分镜与prompt/publish.md` — 全文重构（6305 字符 / 7 节 / 11 个可复制围栏）

### 升格为通用规则
- `.claude/agent_refs/project/ai_video.md` rule 16（16.1–16.7）：落点、七节结构、按站分语言、围栏纪律、四站字段上限表、metadata 必填项、竖版取舍、改 shot 后章节即 stale
- `CLAUDE.md` § AI video rules 的 publish.md bullet 同步
- `ai_videos/huangye_shenghuo/_series/series_bible.md` — 新增系列不变量 10（四站发布页）／11（封面按站分两种）／12（无人声 ⇒ 一条母版通吃四站）

## Follow-up 004 — 2026-09-12 16:20:00
Source: user_input/follow_ups/202609.md - section 004
Summary: shot04 白天刨土的镜出现火星 —— 两个独立成因，都修了。

成因（两条，缺一条都不会出这个 bug）:
1. **措辞**：`情节:`/`分镜:`/`动作:` 三处都写着「几块**被烧得发红发脆**的石头」。
   本意是多年前一场火留下的烧痕，模型按**当前状态**画成了红热冒星的石头——汉语没有过去时，
   而「状态」才有画面。
2. **风格串污染**：`渲染样式:` 全 12 镜共用同一条串，里面写死了
   「冷白灰环境与**暖橙火光**强色温对比」。那是给夜里有火的 shot10–12 定的调，
   却对**每一镜**都在要求暖橙火光。本片受污染 6 镜（shot01/02/03/04/07/09）。
   而且 `NEG_FIRE` 只在 `fire` 为真时才挂——**无火的镜反而一条灭火负向都没有**。

Auto-updated:
- `tools/gen_shots_hy2.py` — shot04 措辞改写：石头改为「表面是暗红褐色的烧斑与细裂纹，
  **早已冷透多年，不发光、不发烫、不冒烟、不掉火星**」；「用手背拨」改为
  「**徒手**把它们拨到坑的一角——**徒手直接碰得到，因为它们是凉的**」（让动作替描述作证）；
  `光线:` 行补「**画面里没有任何暖色光源**……只是被天光照亮的两种普通质感」。
- `tools/gen_shots_hy2.py` — `STYLE` 拆成 `STYLE_BASE` + `STYLE_FIRE` / `STYLE_NOFIRE`，
  新增 `style_of(fire)`。**无火的九镜现在反向声明**「本镜画面里没有任何火、也没有任何暖色光源——
  通体冷白灰，不许出现暖橙/橙红的光斑、辉光、反射或补光」。
- `tools/gen_shots_hy2.py` — 新增 `NEG_NOFIRE` 负向组（火星 / 飞溅的火花 / 余烬 / 发光的炭 /
  炭火通红 / 红热的石头 / 石头发烫 / 炽热发光 / 明火 / 火焰 / 火堆 / 冒烟 / 热浪扭曲 /
  暖橙光斑 / 暖色辉光 / 暖色补光），**`fire` 为假的镜一律挂上**。

验证: 全 12 镜风格串正确分流（shot01–09 声明无暖色光源 · shot10–12 保留暖橙火光对比）；
shot04 负向含全部灭火词；K10 最长 2912 ✔；11 个接缝判定未受影响、仍全部强档 ✔

上收为全局规则:
- `.claude/agent_refs/project/ai_video.md` — 新增 **16.8**（历史痕迹不能写成正在发生；
  四步写法：冷材质 + 正面写死状态 + 动作作证 + 负向反面词；推广到「积水的坑 ≠ 正在下雨」
  「烧黑的锅底 ≠ 锅在火上」）与 **16.9**（风格串里的具体光源措辞必须条件化；
  一般化为「任何全片共用串写进去前先问：这句对每一镜都成立吗」）。
- `CLAUDE.md` — rule 16 条目补 ④⑤ 两句。

未处理（留给用户决定）: **hy1 有同一处风格串污染，8 镜受影响**
（shot01–07 / shot09 画面里没有火却都带着「暖橙火光强色温对比」）。
这 8 镜**已有渲染且成片未见异常**，改生成器会让 prompt 与既有渲染脱钩，
按 CLAUDE.md「规则变更不回溯已完成产物」暂不动；若日后重渲 hy1 再一并改。

## Follow-up 005 — 2026-09-12 17:00:00
Source: user_input/follow_ups/202609.md - section 005
Summary: 004 的改动收敛到 shot04 一镜；技术债显式化；补两层防复发机制。

背景: 12 镜此时**全部已出过渲染**，所以 004 里「无火的九镜一律改风格串」的做法
会让 8 条有效渲染的 prompt 与成片脱钩。用户裁定：先只改 shot04。

Auto-updated:
- `tools/gen_shots_hy2.py` — 新增 **`STYLE_LEGACY_NOFIRE = {1,2,3,5,6,7,8,9}`**：
  无火但仍沿用旧风格串的镜。**只减不增** —— 新镜自动走 `style_of()` 拿到反向声明，
  加不进这个清单；重渲某镜时把号码删掉即可。配 `check_style_debt()`
  （校验清单里没有不存在的镜号、没有混进有火的镜）+ 每次 build 打印清单。
- `tools/gen_shots_hy2.py` — **旧风格串恢复为逐字完整常量 `STYLE_FIRE`**。
  第一版把它拆成 `BASE + 分句` 后分句位置变了，导致 12 镜正向块**全部** diff；
  改为条件返回两个**完整**串，其中一个与 2026-09-12 之前逐字相同。
- `NEG_NOFIRE` 同样只挂给不在清单里的无火镜（当前仅 shot04）。

验证: 逐镜比对本轮修改前后的 ```text 块 —— **只有 shot04 的正向与负向变了，
其余 11 镜正向、负向全部逐字未变** ✔；接缝审计 11/11 强档未受影响 ✔；K10 最长 2912 ✔

防复发（两层，与本片历史包袱无关）:
- `.claude/skills/ai_videos__格式契约/SKILL.md` — 新增 **K32**：
  无在燃的火的镜若出现 ①描述该光源的措辞（`暖橙火光` 等）或 ②「正在发生」的热态词
  （`烧红`/`通红`/`余烬` 等）＝ blocker；③负向缺灭火词 / ④共用串含只对部分镜成立的内容 ＝ warning。
  **K32 已拿真实数据验过**：旧 shot04 ①②全中、新 shot04 干净、shot10–12 干净、8 镜债如实报出。
  初版有两处误报，已写进规则作为强制前置步骤 ——
  (a) 「本镜有没有火」取**结构化信号**（Shot context 的「用火视觉底线」块），不靠关键词猜
  （否则夜里真有火的 shot11/12 会被判成无火）；
  (b) 扫描前**剔掉含否定词的分句**（`光线:` 里「**没有**闪电」不算提到闪电）。
- `CLAUDE.md` — 新增生成时 self-check（与 K10、白话自查同级）：写完任何视频 prompt 前自问
  「这一镜真的有这个光源吗」「这个词写的是痕迹还是正在发生」。

Housekeeping: 应用户要求删除 shot04 作废的渲染稿
`renders/jimeng-2026-09-12-3954-shot04 ….mp4`（38.7 MB，带火星那条）。
**该文件未进 assets.json、未推 R2，删除不可恢复** —— 这是有意为之（它已作废，需按新 prompt 重出）。

### 005 补记 — 2026-09-12 17:40：防复发从「写下来」变成「跑得动」
用户复述了两条要求（只改 shot04 · 确保其他剧本其他 shot 不再犯），据此把防复发落成可执行的：

- **新增 `tools/prompt_light.py`** —— K32 的可执行实现，全仓唯一一份。
  `python tools/prompt_light.py [范围]` 巡检；`prompt_light.gate(emitted, legacy={...})` 做构建闸门。
- **两个生成器都接上闸门**：`gen_shots_hy2.py`（legacy ＝ 除 shot04 外的 11 镜）、
  `gen_shots_hy1.py`（legacy ＝ 全 12 镜，已全部出过渲染、用户裁定不动）。
  **清单只减不增**——负向测试确认：新 `shot13` 若沿用带光源的共用串，**构建直接终止**。
- 全仓巡检结果（7 部剧 / 152 镜）：hy1 12/12 · hy2 11/12 有 C1 债（均已进 legacy）；
  C2 warning 共 4 条（hy1 shot12 的「通红」、xingji_yingjiu 三镜的「余烬」），
  读原句判断均为**真的在燃**，不是痕迹误写，无需改。

两处精度教训（已写进模块注释，因为它们正是这类检查最容易翻车的地方）:
1. **C2 只能是 warning。** 「本镜有没有在燃的火」没有跨剧通用的结构化信号
   （`用火视觉底线` 只有 hy2 的生成器 emit）。初版拿关键词猜、并做成 blocker，
   结果在 413 个 shot 上误报一片——**大面积报警的检查等于没有检查**。
2. **C1 的判据不能带「多少镜中招」的条件。** 初版写成「该 token 出现在每一镜才报」，
   想借此避免误伤；结果 hy2 修好 shot04 后变成 11/12，**部分污染直接逃掉**，而债一点没少。
   现判据与中招镜数无关：`渲染样式:` 行里出现任何点名光源的词即 blocker。

另修：扫描排除 `shotlist.md`（它也以 shot 开头，但是清单不是镜）与 `_deleted/`、`previz/`、`renders/`。

## Follow-up 006 — 2026-09-12 19:10:00
Source: user_input/follow_ups/202609.md - section 006
Summary: shot05 状态倒退 + 橡皮手；两者同源，从流程上解决。

诊断: 看着是两个 bug，其实是一个 —— **prompt 写满了「他在做什么」，却几乎没写
「此刻它是什么样」**。视频模型逐时刻渲染，不写死状态，状态就会漂；镜内切镜把它放大。

Auto-updated（只动 shot05）:
- `tools/gen_shots_hy2.py` — shot05 新增 **`sstate` 逐段状态账本**（5 段，单调前进），
  emit 成 prompt 里的 `镜内状态:` 行。
- `tools/gen_shots_hy2.py` — **shot05 分段重排**：剖视段由 12–18s 收成 12–16s 且
  **明确限定为「只看已经挖好的那一段直沟，画面尽头沟戛然而止、前方是没动过的原土，
  本段没有任何人在挖、也没有手入画」**；折向绕壁段顺延到 16–21s 并写明
  「镜头里那条直沟仍然在、原样不动」。
  **这处矛盾是补账本时才暴露的** —— 原脚本第三段展示成品、第四段又在挖，脚本自身就前后矛盾。
- `tools/gen_shots_hy2.py` — shot05 `走位:` 末段改写：**身体与洞口相距约一臂、半跪在正前方、
  肩到手是一条完整可见的连线、绝不允许只出现一只脱离身体的手**；
  `动作:` 末段拆成先后 —— **每塞完一块手都完全退出洞口；先收手、在裤腿上抹一下，
  然后才撑着膝盖起身退开；收手与起身绝不同时发生**。
- 新增负向组 `NEG_REGRESS`（建造进度倒退）与 `NEG_LIMB`（橡皮手），按 `sstate` / `limb` 旗标挂。
- **shot05 退出 `STYLE_LEGACY_NOFIRE`** —— 既然要重渲，就把 shot04 那轮的风格串债一并还掉。

验证: 累计 prompt 变动仍只有 **shot04、shot05** 两镜；正向 3528 字（上限 5000）；
11 个接缝判定未受影响。

流程化（这是本条的重点）:
- **新增 `tools/shot_logic.py`** —— K33 的可执行实现。
  L1（blocker）：`分镜:` ≥2 段 + 有建造动词却无 `镜内状态:` 账本，或账本段数与分镜对不上。
  L2（warning）：同段内既有探入动词又有位移动词却无次序词，附原句。
- **三个生成器全部接上** `shot_logic.gate(emitted, legacy=...)`：
  hy1 全 12 镜豁免（已渲染）、hy2 除 shot05 外豁免、
  **hy3 挂的是 `LEDGER_TODO`（11 镜）而不是豁免** —— hy3 一条渲染都没出，
  这些是**出片前必须补完**的待办，补一镜删一个号，每次 build 打印。
- 规则上收：`ai_video.md` **16.10**（逐段状态账本）· **16.11**（肢体与位移写死先后 + 身体与开口的几何）·
  **16.12**（这类契约做成闸门 + 巡检，并区分「技术债豁免」与「LEDGER_TODO 待办」两种 legacy 语义）；
  `格式契约` **K33**；`CLAUDE.md` 新增镜内自洽 self-check。

**已删（用户 2026-09-13 确认）**: hy2 shot05 的旧渲染稿（带两个 bug）。
删除时发现两件事，记下来因为它们改变了「删一条渲染」的标准动作：
① **每一镜其实有两份同样的字节** —— `renders/jimeng-….mp4`（下载原件）与
   `shotNN.mp4`（规范名副本），sha256 相同。只删 `renders/` 下那份，
   **规范名的那份会以旧内容留在原地**，后续拼接会悄悄用到过期素材。
   （上一轮删 shot04 时只删了 `renders/` 下的；事后核对 shot04 无恙——
   用户随后重渲并覆盖了两份，但那是运气，不是流程。）
② **shot05 与 shot04 不同，它已经推上了 R2**（`assets.json` 里有两条）。
   只删本地不够 —— 下一次 `assets_sync pull` 会把它拉回来。
   所以同步从 `assets.json` 摘除了那两条（1659 → 1657）。
   R2 上的对象暂成孤儿，等哪天 `assets_sync.py prune` 回收；
   **在那之前它仍可恢复**，这点与 shot04（从未推送、删即永久）正相反。
hy3 的 11 镜账本尚未编写。
