# 追加指令日志 · shengji_zhilu · 2026-09

本文件按 `CLAUDE.md` § Follow-up prompt handling 追加，**只增不改**。
`revised_prompt.md` ＝ `raw_prompt.md` + 本文件全部 `## NNN` 段，按序拼接。

---

## 001 — 2026-09-19 23:15:00 — 要一棵完整的世界地理树

> target_stage: 0
> target_artifacts:
>   - 0_research/map/world_tree.md
> severity: medium

### 抽象后的指令

在往下走之前，先交付一份**从世界一路递进到每一个地块与城镇**的魔兽世界地理清单，
组织形态为**文件夹树**：最上层是世界，其次是大陆，然后逐层递进。
目的是让用户确认「Claude 手上确实有足够的地理 context」。

### 落地

- 层级定为六层：世界 → 大陆 → 分区（lore region）→ 地区（zone）→ 子区域 → 聚居点·地标。
- 副本 / 团本 / 战场 / 飞行点 / 码头 / 地铁站**挂在入口所在地**，不另开一棵树。
- **树数据的唯一出处是片区 YAML**；`world_tree.md` / `world_overview.md` / `zones/*.md` / `world_tree.json`
  全部由 `tools/build_world_tree.py` 生成，手改无效（rule 4i ①）。
- 该脚本是**闸门**：id 重复 / parent 不存在 / 成环 / 必填缺失 / 枚举越界 → exit 1 不写盘。
- 精度分级：本剧要拍的四区细到农舍与桥；北郡 + 闪金镇细到每栋房子的材质与屋顶；远期区域先到子区域层。

### 一句话

先交一棵可验真的六层世界地理树，数据存 YAML、文档靠生成、脚本当闸门。

---

## 002 — 2026-09-19 23:39:00 — 授权全自主推进

> target_stage: 0
> severity: high

### 抽象后的指令

用户离线休息。**授权在无人值守下尽可能多地推进流水线**：
- **不得提问**（等同 `# EXECUTION MODE: AUTONOMOUS`）；
- 遇到不确定的一律自行做 best-effort 决策；
- **包括删除文件这类高风险操作在内，一律自行决定，不回头请示**。

### 落地

- 本次运行全程按 AUTONOMOUS 契约执行：不调用 `AskUserQuestion`；
  每个判断以 `*(judgment call — 选 X 因为 Y)*` **内联记在产出物里**，形成自解释轨迹。
- 迭代上限照旧（单工作单元 3 轮修订 / 访谈 3 轮 / 同一问题跨两轮复现即熔断），
  触顶则写 `pipeline.halted` 并停，不静默重试。
- 其余全部规则不变（状态面、审计、每阶段 QC 关卡、每次 update 复核）。
- **不做 git commit / push**（用户未授权提交；产物留在工作区，早上可整体审阅或丢弃）。

### 一句话

无人值守期间全自主推进流水线，判断内联留痕，不提交 git。

---

## 003 — 2026-09-21 — 航线俯视图规则跨 session 传达 + 3D 层决策延后

> target_stage: 5
> target_artifacts:
>   - 5_6_分镜与prompt/episodes/epNN/shots/shotNN/planning/
> severity: medium

### 抽象后的指令

用户在**另一个 session** 定下了 `ai_video.md` rule 4j「航线俯视图」（2026-09-20），
并已落到四个状态面（`CLAUDE.md` 六阶段表 · `agent_refs/project/ai_video.md` 4j ·
`stage5_分镜` playbook 4b-2 · 工具 `tools/shot_floorplan.py` + `tools/previz/city_layout.py`）。
本 session 确认**能读到**——规则落状态面而不是留在对话里，跨 session 才传得过来。

**规则要点**：机位在世界里走动的镜（航拍 / 穿行 / 长距离移动 / 跨地点），
**顺序是硬的：floor plan → 用户点头 → 才建 previz、才渲**。原地推拉摇不必。

### 用户裁定

对「本剧第一季要不要上 3D 层」这个前置问题，用户答复 **「等之后再说」——延后，不在本轮决定**。

### 为什么它是前置问题

`shot_floorplan.py` 的数据源是「`previz_config.toml` 的 `[[机位]]` + 世界地理坐标表」，
**脚本不自编任何坐标**。而本剧目前没有任何 `.blend`、没有坐标表、没有 `previz_config.toml`——
sk2 虽有整城暴风城 blend，但本剧第一季主战场（北郡山谷 / 闪金镇 / 艾尔文 / 西部荒野）**一处都没建**。

### 受影响的镜（排到该集前必须回来定）

| 集 | 镜 | 为什么算走动镜 |
|---|---|---|
| **ep05** | S02–S03 从西泉要塞一路南下穿河爪营地上霍格山 | 跨地点长距移动 |
| **ep11** | 第一次坐狮鹫（起飞 + 航拍） | 航拍 |
| ep12 | 麦田 / 风车 / 收割机的穿行 | 待排镜后判 |

两条路：**(a) 上 3D** —— 为艾尔文那一片建 blend + 坐标表，再谈 floor plan；
**(b) 第一季不上 3D** —— ep11 的狮鹫航拍要重新设计成不依赖精确航线的拍法。

### 一句话

rule 4j 已确认可跨 session 读到；本剧的 3D 层决策延后，ep05 / ep11 排镜前必须回来定。

---

## 004 — 2026-09-21 — 四项拍板：并行推进 / 3D 只上北郡+闪金 / 纯爱好者不变现 / 剧名占位

> target_stage: 6
> target_artifacts:
>   - 1_立项/concept.md
>   - 0_research/map/PIPELINE_3D.md
>   - pending_user.md
> severity: medium

用户就四个待决项给出答复：

### 1. 下一步 ＝ 两条并行
④ 建北郡 `.blend` + 校验渲图，与 阶段 4 写 ep01–03 剧本**同时推进**。
理由（用户选项原意）：前者是脚本跑、后者是写字，互不抢资源。

### 2. 第一季 3D 层范围 ＝ 只上北郡 + 闪金镇（pending #8 结案）
- **北郡山谷**：建 `scenes/bg1_北郡山谷/_blender/bg1.blend`（ep01–ep03）
- **闪金镇**：建 blend（ep04–ep07）
- **暴风城**：不新建，**直接复用 sk2 的整城 blend**（`tools/build_stormwind.py`），
  只在 ep08 真要拍光明大教堂那一段时扩建该段走廊
- **哨兵岭 / 西部荒野（ep11–ep16）**：第一季不建。
  ep11 的狮鹫航拍到时单独决策，不在本次范围内。

### 3. 版权姿态 ＝ 纯爱好者 · 不变现（concept E6 / F1 维持原判）
片内用原名（暴风城 / 圣骑士 / 艾尔文森林），不接商单、不开会员、不挂带货。
E6 从「Claude 代拍板待复核」转为**用户确认**。

### 4. 剧名 ＝ 先不定，《圣光刚好够用》作占位
等前三集剧本写出来、调性定型后再取。仓库内所有出现处继续用占位名，
**但不得进入任何对外文案**（publish.md 的标题块留 TODO）。

**一句话**：3D 只铺前 7 集的两个地点、暴风城吃 sk2 的现成城；版权走最保守档；剧名延后；建模与写剧本并行开跑。

---

## 005 — 2026-09-22 — 场地平面图（floor plan）成为每个 bg 的必备产物

> target_stage: 2
> target_artifacts:
>   - 2_世界观人设/scenes/bg*/planning/blocks.toml
>   - 0_research/map/PIPELINE_3D.md
> severity: medium

用户指令（抽象后）：**每个 scene 里的每个 bg 都应当有一份 floor plan**，图上标出该场景里的
所有建筑物、编号 1/2/3/4；floor plan 是必备产物，且它是下游的上游——
**先有它，才谈 shot 的 flight plan、Blender previz MP4，最终指导 Seedance 出片**。
同时要求给出「什么样的 floor plan 算好、标准是什么」。

### 落点

标准本身是**通用规则**，已写进 `.claude/agent_refs/project/ai_video.md` **rule 4k**
+ `CLAUDE.md` 指针，可执行版是 `tools/previz/planschema.py`。本剧这一侧的执行见 changelog。

### 与本剧相关的三条

1. 本剧 9 个 bg **早已全部有图**（2026-09-21 那轮建的），本次是按新标准补字段并重出。
2. 新增必填 `id` / `h_m` / `scale_src` 已补齐；`absent`（这里没有什么）补了 6 个 bg，
   内容来自各 toml 原本躺在注释里的 ❌ 清单——**它是本图唯一直接喂到 Seedance 负面词的东西**。
3. 本剧没有状态变体 bg，所以 `states` / `plan.toml` 机制在本剧不触发；它是给 `hy1` 那类
   「同一块地的三个建造阶段」准备的。

**一句话**：floor plan 升为每个 bg 的必备上游产物；标准落成 rule 4k 与 `planschema.py`；本剧 9 张图按新标准重出。

---

## 006 — 2026-09-22 22:44:53 — 两片大陆全图入 scene：区目录 + 每个聚居点/子区域一个 bg + 原版地图 ref + 全部 floor plan

> target_stage: 2
> target_artifacts:
>   - 0_research/map/world_tree.json
>   - 0_research/map/refs/
>   - 2_世界观人设/scenes/
> severity: high

### 指令（抽象后）

1. **完整性先行**：先做一遍调研，确定东部王国与卡利姆多（经典旧世）到底包含哪些地图（区 / 主城）与城镇，
   形成一份可核对的「完整清单」——没有清单就无法判断「做完了」。
2. **全部入 scene**：清单上的每一个地图与城镇都要在 `2_世界观人设/scenes/` 里有落点。
3. **scene 目录按魔兽的层级排**：与游戏内地图一致（大陆 → 区 → 子区 / 城镇）。
4. **每个 scene 的 ref 地图 ＝ 下载好的原版魔兽地图**：每个区目录都要能看到对应的原版区图。
5. **最后给每一个 scene 与每一个 bg 出 floor plan 图**（rule 4k）。

### 四项拍板（AskUserQuestion，用户全部选推荐项）

| 项 | 裁定 |
|---|---|
| 颗粒度 | **区 = scene 目录**（42 区 + 8 主城）；**每个具名聚居点 + 每个具名野外子区域 = 一个 bg**；室内房间 / 副本内部段落不立 bg；地标 (POI) 不立 bg |
| 目录层级 | `scenes/{大陆}/{区}/bg{N}_{主体}/`（三层，与游戏内地图层级一致；主城作为独立区目录挂大陆下）；同步改 webapp 场景扫描支持嵌套 |
| 旧 bg | 已建好的 9 个（bg1/2/3/17–22）**迁入**新布局（`scenes/eastern_kingdoms/elwynn_forest/`），blend/glb 一并搬，所有引用同步改 |
| 产物深度 | 每个 bg：主体卡（≥1500 字锚点 prompt，挂 bg1 世界锚点）+ 原版地图 ref（`.link.json` 指向 `0_research/map/refs/` 单一副本 + 该点位裁切图）+ `planning/blocks.toml` + floor plan PNG；**不建 blend/glb**（沿用 follow-up 004：3D 只上北郡 + 闪金镇） |

**一句话**：把整个艾泽拉斯两片大陆按「大陆/区/bg」铺进 scenes，每区带原版地图 ref，每区与每个 bg 都有 floor plan；3D 不扩。

---

## 007 — 2026-09-24 12:40:00 — 平面图上每个编号块都要有资产卡与图，再出 GLB，最后拼成 bg 的 blend

> target_stage: 2
> target_artifacts:
>   - 2_世界观人设/scenes/{大陆}/{区}/bg{N}_*/planning/blocks.toml
>   - 2_世界观人设/scenes/{大陆}/{区}/_assets/
>   - tools/build_scene.py
> severity: high

### 指令（抽象后）

1. floor plan 上的每个编号块（圆牌 1、2、3…＝ `[[block]]`）都要有一份**可生成它的资产卡与 prompt**。
2. 出图 ＝ **网上参考图 + prompt**；出图工具先用即梦 `dreamina` CLI，不行再用 ElevenLabs（`tools/image_fetch.py`）。
3. 每个资产出**正 / 侧 / 背**三视图，喂 **Hyper3D（Rodin）** 出 GLB。
4. 再把该 bg 的全部资产图 + GLB + prompt + 参考图合成**这个 bg 的 `.blend`**。
5. 可以对已出平面图的 bg 并行做，怎么并行自定。

### Claude 的执行裁定（用户授权自主决定，2026-09-24）

| 项 | 裁定 | 理由 |
|---|---|---|
| 「网上参考图」 | 只用**真实世界、许可干净**的照片（Wikimedia Commons，逐张记作者 / 许可） | divergence #8 / #108：权利人的官方图与游戏截图不进任何生成模型——这条不变；真实照片与它不冲突 |
| 资产去重 | 资产住区级库 `scenes/{大陆}/{区}/_assets/a{NN}_{名}/`，块用 `asset = "a{NN}"` 引用；同一区的民居 / 帐篷 / 火塘 / 立石只出一份 | rule 4i ①（副本必漂）；块总数约 8800，逐块各出一份不可行 |
| 线性结构 | 城墙 / 木栅 / 寨墙 / 围栏 / 道路 / 地形仍走 `build_scene.py` 脚本 kind，不做 image-to-3D | rule 4g ①②：生成式给不出直角与连续长墙 |
| 成片块（橡树林 / 帐篷群） | 出**一个代表性单体**，在块的占地里确定性散布 | 一片 160 m 的林子不是一个物件 |
| 额度 | Hyper3D 余额 155 credits（2026-09-24 实测）是瓶颈：先做一个 bg 试点量出单价，再按第一季优先级分配 | — |

**一句话**：每个编号块 → 区级资产卡（真实照片参考 + prompt）→ 即梦三视图 → Rodin GLB → `build_scene.py` 的 `asset` kind 拼成 bg 的 blend；先试点、再按额度排优先级。

---

## 008 — 2026-09-24 22:48:32 — 转向剧集叙事：无玩家概念 · 每集约 10 分钟 · 先做第一集

> target_stage: 1
> target_artifacts:
>   - 1_立项/concept.md
>   - 3_大纲/arc_outline.md
>   - 4_剧本/episodes/ep01/script.md
>   - divergence.md
> severity: high

### 指令（抽象后）

1. **不出现「游戏 / 玩家」概念。** 取材是「一个人族圣骑士新手在魔兽世界里练级」，但片子是**一部世界内的剧集**：
   一个新人来到这个世界、探索、成长。全程不说明这是游戏，没有玩家、UI、复活跑尸。
2. **单集约 10 分钟**（原 90–120s 作废）。
3. **敏捷推进**：先只做第一集，看反馈再定后面怎么走；主线跟着魔兽原典走，可以杜撰情节让故事饱满，有好提议就拿出来讨论。
4. 结伴、组队、任务、探索、华丽战斗都要有（全季尺度）。

### 第一集四项拍板（多选题）

| 问题 | 用户选择 |
|---|---|
| 主角身份 | **不存在玩家概念**：就是一个新人探索世界、成长的剧集（取代「回归玩家」与「新手玩家」两个选项） |
| 覆盖范围 | **北郡全程，出谷为止**（1→5/6 级；旧 ep01–03 合并后扩写） |
| 其他同伴 | **认识一个同期新人，结伴** |
| 华丽战斗 | **不刻意**，第一集用加瑞克那场 BOSS 战收尾，尽量打出打击感 |

**一句话**：剧集化、零玩家概念、每集 10 分钟；第一集＝北郡全程，结识一个同期新兵，以加瑞克之战为高潮，走出南墙隘口收尾。

---

## 009 — 2026-09-24 23:51:06 — 面向英语观众：观众可见的一切用英文

> target_stage: 1
> target_artifacts:
>   - 1_立项/concept.md
>   - 4_剧本/episodes/ep01/script.md
>   - divergence.md
>   - tools/script_tools.py
> severity: high

### 指令（抽象后）

1. 本剧**面向英语系观众**：场景、文化语境、对白一律英文——人名 / 地名 / 组织名用**官方英文原名**，笑点与说话方式按英语观众写（地道口语，不是中文翻译腔）。
2. 剧本文件的语言分工（多选题拍板）：**台词英文，每句下附一行中文意思供审稿；画面动作、备注、给出图模型的 prompt 仍用中文**。

**一句话**：观众听到、看到的全是英文；工作文档与出图 prompt 仍是中文。

---

## 010 — 2026-09-24 23:57:53 — 夜间全自主推进（含删除决定）

> target_stage: 4
> severity: medium

### 指令（抽象后）

用户离开期间：尽可能多地推进后续步骤；**不问问题**，不确定处自行做 best-effort 决定并留痕（`*(judgment call — …)*`）；删除文件这类高风险决定也自行做。

**一句话**：AUTONOMOUS 模式，范围＝ep01 定稿之后能想到的全部后续步骤。

---

## 011 — 2026-09-25 08:07:58 — 每到一个新地方，留足镜头展示场景

> target_stage: 4
> target_artifacts:
>   - 4_剧本/episodes/ep01/script.md
>   - 5_6_分镜与prompt/episodes/ep01/
>   - tools/script_tools.py
> severity: medium

### 指令（抽象后）

剧情每到一个新地方，都要留有足够的镜头给场景本身，把这个地方的特点拍出来——自然、优美或宏大。
展示的方式灵活：主角环顾四周、走进去让空间在身边展开、升降 / 航拍揭示、前景遮挡后拉开、当地人带着看，等等。

### 落地（Claude 裁定）

- 做成剧本闸门：每个场景主体（bg）在全剧**第一次出现**的那一镜，必须有 `- 场景展示:` 行（时间窗 + 方式 + 展示什么特点）；
  **新地区（区）≥ 12 秒、区内新地点 ≥ 6 秒**；闪前镜不算抵达。阈值写在 `4_剧本/script.toml` 的 `[scenery]`。
- 回改 ep01：进谷、修道院、主厅、林缘、葡萄园补足展示时间（全集 631s → 652s）。

**一句话**：新地方先给景，再演戏；够不够由闸门说了算。

---

## 012 — 2026-09-25 08:20:25 — GLB 只装单个物体；每个场景都要出图；blend 汇总图 + prompt + 物体 GLB

> target_stage: 2
> target_artifacts:
>   - tools/build_scene.py
>   - tools/gen_bg_images.py
>   - 2_世界观人设/scenes/**/_assets/plan.toml
> severity: high

### 指令（抽象后）

1. **一个 GLB 只用来生成单个物体**（一栋建筑、一件器物、一张桌子）。一个 bg 里有多栋建筑或多件物体，是 Blender 的活，不是 GLB 的活——**以后绝不再给场景出 GLB**；已出的场景级 GLB 作废。
2. **每个场景都应该有图片生成**（场景图 + 方位 plate 图）。
3. **图片、prompt、场景内每个物体的 GLB 汇总到一起，生成场景的 blend。**

**一句话**：物体 → GLB，场景 → 图 + blend；blend 由图、prompt 与单物体 GLB 拼出来。

---

## 013 — 2026-09-25 09:21:32 — 区级资产库并进 props；场景引用 prop 出图与建 blend

> target_stage: 2
> target_artifacts:
>   - 2_世界观人设/props/
>   - 2_世界观人设/scenes/**/_assets/
>   - tools/gen_bg_assets.py
>   - tools/build_scene.py
>   - tools/gen_bg_images.py
> severity: high

### 指令（抽象后）

1. 各区 `_assets/` 里的物件（三视图 + GLB + 卡 + 参考照片）**统一搬进本剧 `props/`**，按 prop 规则重命名（`p{N}_{名}`，全剧一份编号，接在剧情物件 p1–p14 之后）。
2. 每个场景（bg）**引用 props 里的物件**来出自己的场景图、建自己的 blend。
3. 拍板（多选题）：
   - **重复物件只合并「同一件东西」**：形制与状态都相同的合成一个 prop（保留已有网格的那件）；区域风格或状态不同的各留一件。
   - **ep01 的 41 张场景图按新方式重出**：出图时挂场景里主要物件的 prop 正面图作参考，让场景图里的物件与 GLB 长得一致。

**一句话**：物件只住 props、全剧一个编号；场景只引用，不自带资产库。
