# 待用户处理 · shengji_zhilu

无人值守期间 Claude 做不了、必须人在电脑前操作的事。**均不阻塞阶段 1–3**；标 🔴 的会在阶段 5/6 出图前变成 blocker。

| # | 事项 | 为什么必须是人 | 收益 | 阻塞点 |
|---|---|---|---|---|
| 1 | 🔴 **截 5 张中文 Classic 客户端图**：角色面板 / 天赋面板 / 声望面板 / 拾取窗口 / 死亡界面 | 灰机 wiki 全站 403，数据库不展示 UI 串；网上找到的都是 T3 | 10 分钟换十几条 T0。`经验条 / 等级 / 天赋 / 声望 / 仇恨 / 跑尸 / 拾取` 这些词**每集都要用**，出镜频率远高于任何一个 NPC 名 | 不做则这些词停在 `ai_draft`，永远不能拍 UI 特写镜，而"经验条涨满"恰恰是本题材最自然的爽点镜之一 |
| 2 | 🔴 **IP 专名 text-only 实测**：拿「暴风城」「圣骑士」「艾尔文森林」各跑一次生成 | 需要在生成平台上实际提交 | 一次实测几分钟，猜错的成本是整批返工 + 诊断困难（到底是名字被拒还是内容被拒？） | 决定 concept C3 的黄级白名单放不放行。**这次实测同时反哺 sk2**——它用同样的写法也还没验过 |
| 3 | **导出 `wago.tools` 的 `LightData` / `Light` CSV**（锁 Classic Era 构建） | 需要浏览器登录导出 | 一次导出可把**全部六区**色板一次性升到数据级——比截图更准、更快、可复核 | 目前只把北郡 + 艾尔文两区按推定开工（concept C4 分区策略），其余四区留 ⚠️。**色板是全剧一致性的底座，底座返工的代价是全剧** |
| 4 | **取一次暴雪 Video Policy / Fan Content Policy 原页** | 反爬；且这条要 `verified_by: human` 才够格支撑发布决策 | 把 IP 合规从 `ai_draft` 升到 `human` | 正式对外发布前必须做。阶段 1–6 不受阻 |
| 5 | **官方简中词表** | 灰机 wiki 403 | 现有译名表有一部分是中文社区通行口径（T3）。六组并存写法已按最通行的一支锁死（`w04` §15） | 不阻塞。日后拿到官方词表作一次**独立迁移任务**批改，不做机械全仓 sweep |
| 6 | **《无限》上线后的画面升级对账**（2026-11-05） | 需要登录新客户端比对 | 若它真的只动光照不动地形，后续集可把参考图整体换成新客户端截图，画质白捡一档 | 不阻塞。**这本身是一个可以公开做的「剧集进化」叙事** |
| 7 | 🔴 **在 Classic Era 建一个人族圣骑士，截 5 张图**：角色特写 / 修道院正面 / 葡萄园 / 矿洞内 / 南出口关卡 | 核验推翻了支撑「1 级圣骑士穿什么」的全部理由（侍从背心是 4.0.3a 新增、Vanilla 板甲要 40 级），**考据里现在一个可直接用的字都没有** | 这 5 张图同时是参考图库的种子（rule 4d「图先行」）| **阻塞 1 级主角的形象定稿图**，不阻塞写卡与写剧本（concept F11） |

## Claude 已代为拍板、醒来后值得优先复核的 6 条

按"改起来最贵"排序——越靠上，越晚改越痛。

1. **B1 主角框架 ＝ 自知玩家 · 现实零画面**（concept B1）。这条定死了全剧的类型与资产预算，改它等于重做立项。
2. **B3 游戏 UI 三层归属制**（concept B3）。它牵动每一个 shot 的两个新字段（`视点:` / `叠层:`），改它要重跑全部分镜。
3. **C1 渲染三档滑杆**（concept C1）。色板与比例句的底座，改它要重出全部资产图。
4. ~~E6 版权姿态~~ ✅ **用户已确认（2026-09-21）：纯爱好者 · 不变现。** 片内用原名，不接商单 / 不开会员 / 不挂带货。备胎（改专名做致敬不点名）保留但不启用。
5. ~~剧名~~ 🟡 **用户决定延后（2026-09-21）**：《圣光刚好够用》降级为**占位名**，等 ep01–03 剧本写完、调性定型再取。**占位名不得进入任何对外文案**——`publish.md` 的标题块留 TODO。
6. **主角 ID「小满」与四名队友的 ID**（concept 固定队）。同上，改名成本低但会扩散到每一份人物卡与台词。

## 本轮 AUTONOMOUS 运行的已知缺口

- `w18_版权与合规.md` 与 `w02 / w05 / w08` 四路**未写出「给用户的决策点」节**（前者的结论已被 W15 决策 5 / W10 决策 6 覆盖；后三路是纯事实路，无需拍板）。
- 地理测绘 `G10 卡利姆多南部` 那一路**失败未交稿**，已列入补跑队列。该片区（尘泥沼泽 / 千针石林 / 菲拉斯 / 塔纳利斯 / 安戈洛 / 希利苏斯）**不在第一季拍摄范围内**，不阻塞。
- 全部事实的 `verified_by` 最高只到 `ai_read`（Claude 抓到原文页并附 `quote`）。**一条 `human` 都没有**——按 stage0 playbook §5，`human` 需要用户亲自点开原文页核对。抽查清单见 `0_research/dossier.md` §0③。


## 8. ✅ 第一季 3D 层范围 —— 已结案（2026-09-21 follow-up 004）

**只上北郡山谷 + 闪金镇。**

| 地点 | 集数 | 3D | 做法 |
|---|---|---|---|
| 北郡山谷 | ep01–ep03 | ✅ 建 | `tools/build_northshire.py` → `scenes/eastern_kingdoms/elwynn_forest/bg1_北郡山谷/_blender/bg1.blend` |
| 闪金镇 | ep04–ep07 | ✅ 建 | 待建，模数与北郡同一套 |
| 暴风城 | ep08 | ♻️ 复用 | 直接吃 sk2 的整城 blend（`tools/build_stormwind.py`），只在真要拍光明大教堂那段时扩建该段走廊 |
| 哨兵岭 / 西部荒野 | ep11–ep16 | ⬜ 不建 | 第一季不建。ep11 狮鹫航拍到时单独决策 |

原来的 (a)/(b) 两档已作废，本表取代之。
