# changelog · xj2

## 立项 + 剧本 + 八镜分镜 — 2026-09-20
Source: 用户 2026-09-19「你先做完」授权自主推进；xj1 完成后按 episode_map 顺序推进

xj2《一口酒换一式剑法》= ch01 场景 3–5。**8 镜 / 220s = 3 分 40 秒**，硬切只有 shot01 一处。

本集是全剧最适合一镜到底的一集：他为送一趟酒把整座客栈上下走了一个来回
（大堂→门口→大堂→楼梯→回廊→客房→下楼→门口），shot03 上行与 shot06 下行是同一条路的
正反两遍，明度过渡顺序刚好相反。**门槛是本集的空间主题**——三镜里少年始终在门内、
醉道人始终在门外，一步都不跨；这道线要到 xj13 山神庙才被跨过。

新建:
- ai_videos/xianjian_yi/xj2/{1_立项/concept.md, 4_剧本/script.md}
- tools/gen_shots_xj2.py — 八镜数据（薄数据文件，引擎共用）
- _series/scenes/余杭客栈/bg5_客栈门口/ — 三镜在门槛上，bg1–bg4 覆盖不到，单列主体（i2i 挂 bg1）
- _series/characters/c8_酒剑仙/ — 醉道人态；**本集不带剑不带法器**，只在两处露破绽
  （站姿歪而不倒、问「你不是很想学剑吗」时抬眼正看一秒）
- _series/characters/c23_南疆随从/ — shot05 有台词，按闸门必须有锁定串；两名随从共用一卡

对原作的处理 8 条（逐条记在 script.md）。要点：删掉游戏里可反复触发的三句循环对白
（线性叙事里是重复）、把「端酒菜路过醉道士」的可选互动并进 shot07、shot04 补一句应声
（否则头领说完画面空四秒）。名句全部原样保留。

工程侧（对后续 79 集的影响更大）:
- **抽出 `tools/xj_shot_engine.py`**：prompt 版式、闸门、产物回读校验全剧共用一份。
  把引擎复制进每一集，就是让某条规则在第 3 集生效、在第 40 集悄悄消失。
  重构后对 xj1 做了逐字节回归比对，差异只有三处有意改动（参考用法去掉冗余的「时」、
  配音注记加上 casting.md 指向、字数随之 ±1），无结构性回归。
- **dialogue.md 改为生成物**：手写的台词表是每句话的第二份副本，副本必漂（rule 4i ①）。
  现由 shot 数据生成，不可能不一致。为不丢失手写版里的导演注，引擎加了 `dub_note` 字段，
  xj1 的两处（shot01 的「同一声源被翻译」、shot03 的「一去不回要最慢最轻」）已回填。
- 闸门新增：动作时间轴必须铺满镜长、入画角色必须有锁定串、不入画的不得占锁定串、
  叙事字段零 IP 专名、镜号连续、末镜唯一。

未做:
- xj2 的 previz（另一个 session 正在占用 Blender 跑 sk1/sk3，按仓库规矩 previz 批量任务不许重叠跑）
- xj1 previz 构图精修、shot01 梦境乱石坡几何
