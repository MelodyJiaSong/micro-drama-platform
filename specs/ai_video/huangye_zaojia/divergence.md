# Divergence note — `huangye_zaojia`（荒野造家）

本项目为**野外生存建造纪实**，不是剧情短剧，也**不是分集剧**——它是一部约四分钟的独立短片，由 32–40 个 shot 拼接成一条成片。以下 7 项偏离 `agent_refs/project/ai_video.md` 的全局默认，仅作用于本项目，不改动全局。

| # | 全局默认 | 本项目 | 理由 |
|---|---|---|---|
| 1 | 画幅 9:16（rule 7） | **16:9**，且不出竖版 | 用户指定；YouTube 为主阵地，横屏承载雨林大景与尺度反差。rule 7 明确允许 per-project override + divergence note。 |
| 2 | 单集 180–195s（rule 6） | **全片 235–250s（约 4 分钟）** | 用户指定；建造过程需要环境长镜承载沉浸感，压到 3 分钟会牺牲"雨中安逸对比"长镜。`shotlist.md` 的 `时长合计` 行按此区间校验。 |
| 3 | 台词为一等公民 + `## 台词配音 prompt`（rule 12.4 / 12.4-H） | **无台词、无旁白、无 TTS**，shot 不含 `台词:` 与 `台词配音` 块 | 纯环境音是本品类的核心竞争力（零语言门槛，一条母版通吃 YouTube/TikTok/抖音）。 |
| 4 | 人物面部锁定描述符 + Seedream 立绘 + voice_id（rule 4 / 12.1） | **不做人脸一致性锁定**，仅锁「体型 / 服装 / 斗笠 / 砍刀」四项轻描述符 | 用户指示"不用担心一致性，按拍摄要求来，重点是环境和住所"。主角以背影/剪影/中远景/手部特写呈现，正脸极少入画。 |
| 5 | 每集 25–35 镜、名义 5–8s/shot 的 fast-cut 节奏（rule 6） | **32–40 镜**，环境长镜 8–15s 与手部特写 3–5s **混切** | 品类节奏不同：奇观长镜是卖点而非注水；快切只给单个手部动作。仍守 3–15s 单镜上限。 |
| 6 | QC 关卡含 `台词大师` / `白话大师` / `武打设计` | 上述三项 **N/A**（显式跳过并记录） | 无台词、无打斗。其余关卡（格式契约 / 站位朝向 / 运镜 / 动作表演 / 光线色调 / 时长节奏 / 剧情连贯 / 全剧序列 / 奇观镜头 / 特效设计）照跑；`奇观镜头` 在本项目提升为**主关卡**。 |

| 7 | `sub_type=novel` 多集结构，产物落 `episodes/epNN/`（rule 2） | **`sub_type=short` 扁平结构**：`5_6_分镜与prompt/{shotlist.md, shots/shotNN/shotNN.md, all_shot_prompts.md, publish.md}`，**不建 `episodes/` 层**；`4_剧本/script.md` 单份、无 `dialogue.md`（无台词）；`3_大纲/outline.md` 是全片段落结构，不是分集大纲 | 用户 2026-09-06 明确："不要 ep1~ep3，总共就几分钟，多个 shot，最后拼接成一部成片即可"。《荒野造家》是系列名，但每一部都是独立短片，各自一个 `ai_videos/{name}/`；`2_世界观人设/` 的锁定资产跨片复用。**regen scope 只有 `scope=project`**（rule 10：shorts 无 per-episode scope）。 |

## 平台说明

用户原始需求提到 TikTok / 抖音，但阶段 1 明确选择"只做 16:9、不出竖版"。两平台以横屏投放；若后续要竖版精华，走 follow-up 走查，不在本季范围。
