# sk2 · 变更日志

## 开工 — 2026-09-18
Source: user_input/raw_prompt.md（用户 2026-09-18 口头发起 + 两轮多选题确认）
Summary: 《时空旅行》第 2 站立项 · 暴风城（经典旧世 Vanilla）· 系列首个「虚拟城」站；阶段 0 设定考据七路并行开跑。

task_id: `sk2-20260918-130201` · run_dir: `.audit/adhoc_agents/2026-09-18/sk2-20260918-130201/`

新建:
- `ai_videos/shikong_lvxing/sk2/{0_research/parts, 1_立项, 2_世界观人设/{characters,props,scenes}, 3_大纲, 4_剧本, 5_6_分镜与prompt/shots}` — 阶段编号目录，与 sk1 同构
- `specs/ai_video/sk2/user_input/{raw_prompt.md, revised_prompt.md}` — 含两轮多选题的 8 项开工参数
- `specs/ai_video/sk2/divergence.md` — 继承 sk1 的 26 条，新增 sk2 专有 10 条（#101–110）

追加:
- `ai_videos/shikong_lvxing/_series/sources.md` §8 「虚构世界（sk2 起）」 — 虚构世界的 T0–T4 tier 映射表 + 暴风城来源表 + 检索教训；只追加不改已有行

用户确认的开工参数（两轮多选题，2026-09-18）:
- 版本锚点 = 经典旧世 Vanilla · 画风 = 半写实（保形制保配色）· 旅行者 = c4 艾拉 Ella Hart · 考据严格度 = 沿用 sk1 口径
- 当天大事 = 工程日 · 矿道地铁通车 · 片长 = 15 分钟版 · 机构 4 处（王座厅 / 大教堂 / 矿道地铁 / 巫师圣殿）· 整城建模 = 要 · 走廊制

待用户拍板（不阻塞阶段 0–5）:
- **#110 IP 定性与发布策略** — 暴风城是暴雪 IP，四站商业分发 / 开收益是风险点；阶段 6 `publish.md` 之前须定
- **#104 虚构站签名开场句** — 「距今 N 年」在虚构世界不成立，已出草案，阶段 1 立项时确认

已知限制:
- 用户提供的 YouTube 暴风城素材（`roMWEeV2P4U`）**无法读取**：页面为 SPA，WebFetch 只返回页脚；Claude 亦无法观看视频。已登记进 `_series/sources.md` §8.2 备查；需要具体画面时由用户截图提供。
- 本会话 `blender` MCP 连接超时、`cascadeur` 拒连 — 阶段 2 之后的整城建模与动作 previz 需用户重启后才能跑。

No conflicts found in: sk1 的任何产物（站站独立，sk2 不读 sk1 的 specs / 镜头）；`_series/characters/c4_艾拉` 沿用不改。

## Follow-up 001–003 — 2026-09-18（阶段 0 进行中）
Source: user_input/follow_ups/202609.md - sections 001 / 002 / 003
Summary: 阶段 0 设定考据七路交稿并合 dossier；当天大事候选重开（W8）；新增玩家共同记忆讲解点一路（W9）。

Auto-updated:
- `ai_videos/shikong_lvxing/sk2/0_research/dossier.md` — 新建，15 节齐全；§0 含七路交稿统计 / **12 条跨路冲突裁定** / 12 条抽查清单 / 参考图库状态 / 来源损失；§9 §11 标 PENDING 等 W8/W9
- `ai_videos/shikong_lvxing/sk2/0_research/parts/w1–w7_*.md` — 七路事实注册表，**480 条事实**（✅367 / ⚠️80 / ❌33，ai_read 439 / ai_draft 41）
- `specs/ai_video/sk2/user_input/follow_ups/202609.md` — 新建，001 YouTube 素材登记 / 002 当天大事重选 / 003 玩家记忆讲解点
- `specs/ai_video/sk2/user_input/revised_prompt.md` — 重生（= raw + 三条 follow-up）

机检（`python tools/facts_registry.py ai_videos/shikong_lvxing/sk2`）:
- 修复 10 条 schema blocker：W5/W7 自造枚举值 `verified_by: ai_search` / `ai_search_snippet` → **降级 `ai_draft`**（搜索摘要 ≠ 抓到原文页，下游不得进 prompt）；`tag` 的括号限定语搬进 `claim` 不丢信息
- 修复后 **blocker 0**，剩 info 70（`negative` 为空，由黑名单兜底 K-F5）

parent 独立复核（不照单全收 worker 结论）:
- ✅ 蓝底金狮唯一文字出处（W3 对、W6 的「查无出处」已补）
- ✅ 港口为 Patch 3.0.2 而非大灾变（**订正任务书**）
- ✅ 官方中文译名「矿道地铁」（**订正任务书**，全仓改名 11 处）
- ✅ Vanilla 王座＝十岁的安杜因 + 摄政伯瓦尔 + 普瑞斯托；**W5 所引的 3.0.2 补丁说明未能在该页复现，已改挂安杜因条目的更硬原文**
- ✅ 监狱位置两说为伪冲突（运河区下方 ∩ 法师区那段运河最北端）
- ✅ 囚犯暴动确为 Year 25 ADP 当下状态

裁定记录（§0 ②，12 条）: 其中 parent 判断两条——**译名跟版本走，取「安杜因」**（与「纪年＝版本锚点」同一逻辑）；**W2 纹章串与 W3 罩袍串是伪冲突**（盾徽 vs 旗帜罩袍，两串并存）。

阻塞下一阶段的事项:
- **§9 / §11 PENDING** — 当天大事未定，W8/W9 交稿后连同候选一并交用户拍板；**阶段 1 立项不能在此之前开始**
- **形制类零覆盖** — W2/W6 两路独立撞墙：窗/门/屋顶/街灯/招牌/内景/纹章配色等「长什么样」文字来源零记载。解法已定（**图不进模型、图进人眼**），但需用户在 Classic Era 客户端补四组截图

No conflicts found in: sk1 的任何产物；`_series/{blacklist.md, glossary.md, characters/}`（sk2 的黑名单与译名表待阶段 0 收尾后追加）。

## Follow-up 004 + 系列 015 — 2026-09-18（阶段 0 收尾 · 阶段 1 立项）
Source: user_input/follow_ups/202609.md - section 004；系列 follow_ups/202609.md - section 015
Summary: 取消当天大事（试拍形态）；形态定调「旅游 + 解说 + 偶尔的 NPC 互动」；阶段 0 条件通过，阶段 1 立项单落地。

Auto-updated:
- `ai_videos/shikong_lvxing/sk2/0_research/dossier.md` — §1「哪一天」定为**没有大事的普通日**；§11 由候选池改写为定案 + 候选池保留备查；§0 ① 补 W8/W9 两行（**九路合计 643 条事实 / 596 ai_read**）；§0 ② 冲突裁定增至 **18 条**；新增 §2b「玩家共同记忆讲解点」（33 个 + 四条冷知识）
- `ai_videos/shikong_lvxing/sk2/0_research/qc_stage0.md` — **新建**，阶段 0 关卡逐条评定
- `ai_videos/shikong_lvxing/sk2/1_立项/concept.md` — **新建**，阶段 1 立项单（含 17 段一日时间线、三件套、四格、密度合同、签名开场的「无大事日」变体）
- `ai_videos/shikong_lvxing/sk2/0_research/parts/w10_masquerade_lines.md` — 新建（《假面舞会》逐字台词 + 平台合规负向组）；**本集不采用，保留备查**
- `specs/ai_video/sk2/divergence.md` — 新增 #112（取消当天大事）、#113（NPC 互动三档）
- `ai_videos/shikong_lvxing/_series/sources.md` §8.3 — 追加四条检索教训（**wikitext raw 取逐字引文**、Wowhead 两条绕法、**官方 ≠ 版本正确**、数值来源须锁构建号）

阶段 0 QC 评定（`qc_stage0.md`）: **条件通过**
- ✅ 事实层全绿：15 节齐全 · 596 条 ai_read（门槛 20）· 全部带 quote + source_url · 12 条抽查清单 · 物价两锚 + 40 行总表 · blocker 0 · 零 hex
- ❌ **关卡 4 未达标**：画面可截图指认的类别有空档（窗/门/屋顶/街灯/招牌/内景/**纹章正向配色**/王座厅 Vanilla 形制…）——文字来源零覆盖，三路研究员均诚实进「未查」，非执行失误
- ❌ **关卡 7 未达标**：0 个资产建了 `refs.md`
- ⚠️ 关卡 6：70 条 fact 的 `negative` 为空（info 级，K-F5 由黑名单兜底）
- **判断：阶段 1 放行（只依赖事实层），阶段 2 卡闸**——阶段 2 每张资产卡要写 ≥1500 字锚点 prompt 且每个形制词要指得出依据，缺口不补就开等于让模型替我们编暴风城的样子（正是 `myth.011` / `myth.014` 要防的）

补缺方案已实测通过: `curl` 取 `warcraft.wiki.gg/images/{文件名}` HTTP 200；Claude 读图一次填掉四个语汇表空格（街灯形制 / 拱为半圆非尖 / 四坡蓝顶加老虎窗 / 半木结构民居），并**纠正 W2 两处**（墙为大块方石缝宽而浅、屋顶为饱和中蓝非深蓝灰），另读出**「蓝色尖顶母题在三个尺度上重复」**——文字来源无人写过，建议进 `style_guide.md` 第一条。

阻塞下一阶段的事项:
- **参考图库两段补缺**：① Claude 自取 wiki 图库（W2 24 组 + W3 22 组 + W7 8 组）② **用户在 Classic Era 客户端截四组图**（旅店内景 / 卫兵战袍正面 / 王座厅台阶下仰拍 / 民居内景与街面招牌）
- **阶段 1 QC 最后一项待人工确认**：签名开场「无大事日」变体的措辞、工作标题、是否把该变体回写 proposal § H
- blender MCP 本会话连接超时，整城建模需用户重启后才能跑

No conflicts found in: sk1 与其余历史站（#112 / #113 均为 sk2 与虚拟城支线专属）。

## 自主全流程跑（阶段 2–6 + 出图 + 3D + previz）— 2026-09-19
Source: 用户 2026-09-18 深夜指令「把 stage2~6 都跑完，再出图、出 GLB、出 blender 与 previz MP4；完全自己完成，不要问问题，遇到不确定的自己 make best effort decision」
Summary: 阶段 2–6 全部落盘；三条生成流水线跑通并在后台批量执行。

### 阶段 2 · 世界观人设（新建）
- `2_世界观人设/style_guide.md` — **第一条是「蓝色尖顶母题」**（读图得出、文字来源无人写过：街灯顶 / 民居屋顶 / 大教堂尖塔三个尺度重复同一形状，是「一眼认出暴风城」的真正机制）；14 条建筑语汇锁定串 + `STYLE_BASE` + 六组负向基线 + 色温三角的条件化纪律
- `world.md` / `casting.md` / `object_inventory.toml`（22 个物件）
- `scenes/stormwind/` — **13 张场景卡 / 27 个视图**（三路 worker 并行写，机检全过：1200–1950 字、零 hex、裸 `=>@`、画面零文字、可辨主体 ≤8、光色条件化 + 反向声明）
- `characters/` — **16 张人物卡**（A 组 7 位具名 NPC 含 12 维人物灵魂；B 组 9 类典型人物）
- `props/` — **22 张物件卡 + object.toml**（`tools/gen_object_cards.py --drama` 生成）
- `_series/characters/c4_艾拉/` — 追加 **sk2 暴风城平民装态**（路由键 `c4-21`，换装在 S13）

### 阶段 3–6（新建）
- `3_大纲/outline.md` — **44 镜分配表** + 八块覆盖对账 + 三条线收束 + 密度合同 + 景别档设计
- `tools/gen_shots_sk2.py` — **分镜 + 五层 prompt 生成器**（阶段 5/6 合一），八道构建闸门
- `5_6_分镜与prompt/` — 44 个 shot 目录 + `shotlist.md` + `all_shot_prompts.md`
- `4_剧本/{script.md, dialogue.md}` — 中英两条

**生成器闸门全过**：44 镜 / **882 s（14.7 min）**· 单镜 15–30 s · 英文 ≤2.8 词/秒 · prompt ≤5000 字 · 零 hex · `tod` 单调不减（末镜次晨豁免）· **42 对硬切全部满足景别档比值 ≥2.0 且机位标签不同** · 唯一承接对 S01→S02。

### 生成流水线（三条，全部实测跑通）
| 环节 | 工具 | 状态 |
|---|---|---|
| 出图 | `tools/image_fetch.py`（ElevenLabs Flows，gpt-image-2） | **物件图 66/66 完成**；场景图 27 个视图排队中 |
| GLB | `tools/hyper3d_fetch.py`（Rodin HTTP，**无头，不依赖挂掉的 blender MCP**） | 批量进行中 |
| 白模闸门 | `tools/whitemodel_normalize.py --fit stretch` | 已链式排在 previz 之后 |
| 整城 | `tools/build_stormwind.py`（**新建**，走廊制、0.5 比例、确定性生成） | ✅ 873 个物体，包围盒 东 -287~30 / 北 -7~335 / 高 44.2 m |
| previz | `tools/previz_sk2.py`（**新建**，机位由镜表反解，不手摆） | 44 镜批量进行中 |

### 修掉的真错误（都是我自己的）
1. **旅店名写错**：暴风城的旅店是 **The Gilded Rose · 镀金玫瑰**，「狮王之傲」是闪金镇那家。全仓 63 处改名 + `bg12` 目录改名（C 批 worker 指出）
2. **教堂广场「喷泉」口径错**：Vanilla 是**纪念碑、不带喷泉**，「喷泉」是现行版本措辞（B 批 worker 指出）
3. **建城脚本两处几何 bug**：`primitive_cube_add(size=1.0)` 之后按 `size/2` 缩放导致全部体块只有一半大；`transform_apply` 把位置吃掉导致全部物体堆在原点
4. **previz 编码失败**：`--res 720` 算出高度 405（奇数），libx264 的 yuv420p 不吃奇数边——已强制取偶
5. **生成器语法错**：非 ASCII 放进 `b"..."` 字节串字面量

### best-effort 判断（用户授权自主决定，逐条记账）
- **白模闸门走 `--fit stretch`**：`object_inventory.toml` 的尺寸是作者按画面推的估值（多条标 ⚠️），不是实测。生成网格比例与它对不上时，对 previz 有意义的是**占位体积与城市布局槽一致**，而不是让网格迁就一个本来就不确定的数。真要追究比例，改 inventory 重跑，不改闸门。
- **Rodin / 闸门 / previz 三段错开跑**：rule 4h ⑥「previz 批量任务不许重叠跑」——`TaskStop` 杀 shell 不杀已 spawn 的 `blender.exe`。已做成链式脚本，不靠人盯。
- **`gen_object_cards.py` 加 `--drama` 参数**（默认仍 sk1，不破坏既有调用）——它原本硬编码到 sk1。

### 仍未解的（须用户处理）
- **阶段 2 的参考图闸**（`qc_stage0.md` 关卡 7）：文字层的形制缺口已由读图填掉大半（`look_from_images.md`），但**四组 Classic Era 客户端截图仍缺**：① 镀金玫瑰内景（含客房床铺）② 卫兵战袍正面（纹章正向配色零文字出处）③ **王座厅台阶下仰拍**（要塞内部 4.0.3a 整个重做过，**绝不能从 2019 的视频取**）④ 民居内景与街面招牌
- **blender MCP / cascadeur MCP 本会话均连不上** —— 但**整条链路已绕开它们**：Blender 走 CLI，Hyper3D 走 HTTP
- **IP 定性与发布策略**（`divergence #110`）：阶段 6 `publish.md` 之前须定

### 执行结果（2026-09-19 收尾）

| 产物 | 数量 | 状态 |
|---|---|---|
| 物件三视图 | **66/66** | ✅ ElevenLabs Flows（gpt-image-2） |
| 场景锚点图 | **27/27** | ✅ 其中 2 张被内容审核拦过，改写措辞后通过 |
| Rodin GLB | **22/22** | ✅ 无头 HTTP，不依赖挂掉的 blender MCP |
| 白模闸门 | **22/22 通过** | ✅ `--fit stretch` |
| 整城 blend | 1022 物体 | ✅ **资产槽已换成真网格**（街灯 ×142 / 苹果树 ×48 / 月亮井 / 集合石 / 地铁齿轮 / 狮鹫 ×2，1.4–4.4 万顶点） |
| shot previz MP4 | **44/44** | ✅ 换上真道具后正在重跑一轮 |

### 出图内容审核（须人眼验收时注意）
- **`bg1-1` 英雄谷连续两次被拦**，第三次才过。代价是把雕像的「战锤 / 巨剑」**改写成「石雕器具」**——武器形制被抹平，而库德兰的战锤是那尊像的识别特征（`lore.024`）。**已在 `bg1_英雄谷.md` 末尾单列「出图审核记录」**，出图后须比对轮廓；若画错，走人工补画或换服务，**不要把这条永久写成「石雕器具」**。
- `bg3-1` 旧城区被拦一次，原因是墙根三人的**乞讨姿态**。改成「歇脚」后通过——**旧城区的「穷」现在靠建筑与铺地承载，不靠人物姿态，这个改法反而更好**，不算削弱。

### previz 调通过程中修掉的四个真 bug（都记在脚本注释里，防复发）
1. **相机被算到地底下**：负俯仰角 × 长距离 → 39 m 处相机落到 −2.2 m，画面只剩悬空屋顶。改成仰角不压低相机、朝向交给 TrackTo
2. **地面整块被远裁剪面裁掉**：一整块 420×438 的四边形，四个顶点同时在默认 clip_end=100 之外 → 改成 12×12 分块铺地 + 拉开裁剪面
3. **距离没按城市比例换算**：`dist_for()` 解的是真实米，城市是 0.5 比例，相机站在两倍远处
4. **libx264 不吃奇数边**：`--res 720` 算出高度 405，整批编码失败（`--res 640`→360 偶数时却是好的，所以一开始没暴露）

### 环境层面的两条教训（已写进脚本，供后续复用）
- **Git Bash 没有 `pgrep`**：链式等待脚本的 `while pgrep ...` 会立即退出，导致闸门抢在 Rodin 前面跑（本次实际发生，只过了 8 个）。改用**文件数轮询**做同步条件。
- **Python 在 Windows 上 `write_text` 会把 `\n` 写成 `\r\n`**：shell 读出来的路由键带 `\r`，`image_fetch` 全部匹配不上（本次实际发生，27 个场景图第一轮全失败）。写给 shell 消费的清单要显式 `newline="\n"` 或事后 `tr -d '\r'`。
- **`blender.exe` 会残留**：`ai_video.md` rule 4h ⑥ 说的重叠确实发生了（一度 3–4 个进程并存，previz 计数卡死），且 `TaskStop` 杀 shell 不杀它。批次之间必须显式 `taskkill`。

### 整城 blend 返工（2026-09-19，用户指出「看起来很乱，像是没做好」）

用户是对的。俯视校验图一看就是「几条虚线铺在大空地上」，不是城。查下来是**一个根因带出的一连串表象**：

**根因：`bpy.ops.object.transform_apply()` 不写全参数时会把 location 一并烘进网格。** 我早先看到「所有物体堆在原点」，就补了一句 `o.location = loc`——**那是对着正确症状开的错药**：位置因此被加了两次，全城每个物体都跑到它该在位置的**两倍远处**，间距翻倍而尺寸不变。表象是：
- 地面瓦片之间空出整整一个瓦片的缝（俯视看成棋盘格）——我一度以为是渲染伪影，还去加了 2% 重叠去遮，没治到根上
- 走廊被拉长一倍，两侧建筑变成稀疏的虚线
- 实测证据：`G_ground_00_00` 的 `location` 是 `(-269.30, 11.94)`，包围盒中心却在 `-538.6`

**修法**：`transform_apply(location=False, rotation=False, scale=True)`，并删掉那句手动补位。

**顺带做的四件（根因修完才看得出还缺什么）**：
1. **走廊由「每侧一排、按 14–20 m 跳着放」改成「连续街墙」**：贴街一排密排不留缝（step ＝ 开间宽）、后面再加 1–2 排进深、中轴铺出街面几何、高度用位置做确定性抖动避免整排等高
2. **补街区填充**（296 个低精度体块）：走廊制的前提是「镜头看不到的不建」，而**本片 S01/S02 是航拍扫全城**，会直接拍到环内空地。sk1 能用纯走廊制是因为它取消了俯视镜（sk1 divergence #26），我们不能照抄
3. **地标补基座**：W1 的锚点带高程（要塞北端 +26.7 m）而地面是平的，不补基座地标就悬在半空——用户截图里要塞正是飘着的
4. **街墙的底一律落到地面**：同理，`p[2]` 是锚点高程，不补齐整排悬空

**结果**：2537 个物体，地面连成一整块，走廊读得出街网与街墙，地标落地。previz 级体量到位。

**教训**：`transform_apply` 的默认参数在这个 Blender 版本里不是我以为的那样，**写 ops 时不要依赖默认值**。更要紧的是——**看到症状先量再改**：我当时直接补了一句 location 就往下走，把一个位置翻倍的 bug 藏了整整一轮，直到用户说「看起来很乱」才回头查。

### 人物参考图研究（2026-09-19，用户要求「生成 character 照片前先从网上 research 参考」）

**做法**：按 `divergence #108`——**参考图只进人眼、不进生成模型**。两路 worker 找图 → 下载 → **逐张用 Read 亲眼看** → 把看到的形制写成中文锁定串 → 锁定串进 prompt，**图一张都没上传**（七张卡的 `参考:` 行仍是 `无（纯文字自由生成）`，机检通过）。

**A 组（7 位具名 NPC）拿到全部人物的游戏内模型截图后，七张卡里六张的服装描述被推翻**：

| 卡 | 原来写的 | 读图实际 |
|---|---|---|
| `c26` 杜加尔 | 护目镜推在额头、皮飞行服、腰挂飞行绳具、深栗短发 | **护目镜戴在眼上**、绯红制服 + 黄色鹰翼徽记 + 土金肩甲、**无绳具**、姜红中长发 + 八字胡 |
| `c22` 伯瓦尔 | 深蓝金狮罩袍、背后无披风 | **身上没有罪袍**（我把 `style_guide` §3.12 的**旗幡**锁定串错挂到人身上了）、**有明黄披风**（原卡写反） |
| `c23` 普瑞斯托 | 深紫近黑高领曳地长裙 | **品红无袖马甲 + 朱红曳地裙 + 四道灰白束带**，双臂裸露、齐额厚刘海 |
| `c25` 乔纳森 | 右手长剑、白色战马 | **战锤**、**灰白重挽马**、颈口黑毛皮领 |
| `c24` 奥里森 | 米白长裙 + 深色围裙 | **不系围裙**；短袖罩衫 + 宽皮腰封 + 靛蓝长裙（与 `w5 person.018` 冲突，判文字错） |
| `c21` 安杜因 | 蓝白带金边王室长袍 + 短斗篷 | **蓝背心 + 白短袖 + 棕短裤**；新增呆毛与雀斑 |
| `c27` 本尼迪塔斯 | （w3 兜帽 vs w5 主教冠长期冲突） | **三源一致戴兜帽 → 裁定 w3 对、w5 错**；金黄琥珀 + 乳白两色，非通体象牙白 |

**统一判断**：四个模型都带着 NPC 装备（发光小锤 / 出鞘剑盾 / 法杖）——**按「模型噪声」统一不采纳**，四张卡都记了并挂了负向。
**留给编剧的一处分歧**：游戏内安杜因是**站在王座前**不是坐着；本片为反讽仍写「坐」，已在 `c21` 的读图记录里备查。

### 版权处置：参考图不进对象存储
worker 提醒 `ai_videos/` 下的 29 张暴雪版权图会被 pre-commit 钩子推去 R2——**那是再分发**。已在 `tools/assets/scanner.py` 的 `_SKIP_DIRS` 加入 `"ref"`：**任何 `ref/` 目录整体不进 R2**。核过：本地 65 张、同步 0 张。形制结论已转写成 `refs.md` 的锁定串（带 source_url），图随时可按 URL 复现。
（sk1 的参考图是公版 Met CC0 / Commons，已在 manifest 里的条目不受影响——`push` 默认不 prune。）

## Follow-up 006 — 2026-09-19 14:20:00
Source: user_input/follow_ups/202609.md - section 006
Summary: 人物卡补人种锚（3 张东亚脸重出）+ 全部补 4 秒建立视频 prompt。

诊断：**16 张人物卡里 14 张没有任何人种 / 地域锚**，而卡里的中文相貌词自带东亚默认值
（`八字胡` `脸盘方正` `颧骨平` `暖麦色`）。逐张验图后确认**实际塌成东亚脸的是 3 张**：
c23 普瑞斯托（乌黑直发 + 齐额刘海，东亚先验最强）、c33 平民女性、c39 街头孩子（两个）。
未塌的 c24/c25/c26/c34 **全是红发姜发——发色替锚点干了活，属侥幸**。

Auto-updated:
- `2_世界观人设/characters/c21–c39`（15 张） — 开篇加人种锚 + 面部加欧洲骨相解剖特征 + 清洗东亚词库 + 负向兜底
- `2_世界观人设/characters/{c23,c33,c39}` — 人种锚加固（黑发与族裔显式拆开）并**重新出图，逐张验收通过**；旧图留 `*_东亚脸废稿.png`
- `2_世界观人设/characters/c36,c37` — `软布鞋` → `软底尖头便鞋`（「布鞋」指向千层底，且与新加的负向打架）
- `tools/gen_turntables_sk2.py` — **新建**，14 张卡的 4 秒 turntable 章节生成器（同构产物走生成器，rule 4i ①）
- `2_世界观人设/characters/c21–c39`（14 张） — 补 `## turntable 说明`（路由键 `c{N}-2`，1072–1223 字）
- `.claude/agent_refs/project/ai_video.md` — 新增 §22（人种锚 + turntable 两条铁律）

派出两路 worker：c27 本尼迪塔斯整卡返工（更详细研究 + 重出图）、c42 酒馆老板新建卡。

No conflicts found in: `3_大纲/`, `4_剧本/`, `world.md`, `style_guide.md`

## Follow-up 007 — 2026-09-19 14:55:00
Source: user_input/follow_ups/202609.md - section 007
Summary: 分镜 `参考:` 行整层缺失，补齐并做成 build 闸门。

用户看 shot1 时发现的。核过之后确认是**生成器 bug，44 镜全中**，一共五个缺口：
1. **previz 一个都没挂** —— 44 个 `shot{NN}_previz.mp4` 都已渲出躺在盘上，但没进参考行
2. **场景主体 handle 写的是盘上不存在的名字**（`bg1_英雄谷锚点`；实际文件是 `bg1-1.png`）
3. **`bg0` 被生成器显式排除** —— 于是 S01/S02 两个航拍镜连场景参考都没有
4. **22 个物件锚点、15 张 NPC 立绘一个都没挂**
5. **人不入画的 S01/S02/S15 反而挂着主角的人物 entity**（`走位:` 明写「画面里没有人」）

根因：`参考:` 行没有闸门。CLAUDE.md 有「全部 handle ⊆ 全部产物路径」这半句，但只是条目描述、
没做成机检，于是整层漏掉也没人拦。

Auto-updated:
- `tools/gen_shots_sk2.py` — 新增参考层（`BG_VIEW` / `PROP_NAME` / `CHAR_NAME` / `SHOT_REFS`）+
  `ref_items()` / `ref_line()`；改写 `build_prompt()` 的参考行；`## Shot context` 加 `- previz:` 指针
  与「参考上传清单」；新增闸门 `gate_refs()`（行非空 / handle 在盘上有文件 / previz 挂了 /
  至少一个场景主体 / 人不入画则不得挂人物 entity）
- `5_6_分镜与prompt/` — 44 个 shot md + shotlist + all_shot_prompts 全部重生成，闸门全过
- `.claude/agent_refs/project/ai_video.md` — 新增 §23（`参考:` 行完整构成 + 三条硬规矩）

副产物：闸门顺带查出**艾拉的 handle 写错**（写成图片键 `c4-1`，但 `_series/characters/` 下一张图都没有
——她走的是 `divergence #6` 用户自建 Seedance entity），已改为目录名 `c4_艾拉`；
以及 `casting.md` 的 `c41` 面包小贩是**重复键**（c32 的卡里写明它就是 Thomas Miller 那个实例）。

No conflicts found in: `3_大纲/outline.md`, `4_剧本/`

## Follow-up 008 — 2026-09-19 15:30:00
Source: user_input/follow_ups/202609.md - section 008
Summary: 艾拉由英国布里斯托尔改为美国波特兰，口音改通用美音。

**改的是系列卡 `_series/characters/c4_艾拉`（唯一出处），其余全是下游、跟着重生成。**
她原有的音色特征（清亮中高音、语速快、句尾上扬、自嘲）本就极贴美国西海岸年轻人的说话方式，
所以真正动的只有「国别 + 口音」两处，声音性格一个字没改。

选波特兰／太平洋西北而不是再来一个南方——名册里 **c5 妮娅已经是亚特兰大南方拖音、温暖中低音**，
两个美国人必须在地域与音区上分得开，否则名册的「六选一」就少了一档。
**副作用（已记录、非阻塞）**：名册的「英国档」现在空着，c1–c3 普通话 / c4 美音 / c5 美音南方 / c6 西班牙口音英语。

Auto-updated:
- `_series/characters/c4_艾拉/c4_艾拉.md` — 标题、声音锁定串、`tone_en`、年龄处境、两处 turntable 人种锚（英国白人→美国白人）共 10 处
- `2_世界观人设/casting.md` §1 — 姓名与音色行；并注明与 c5 妮娅的区分依据
- `1_立项/concept.md`、`proposal.md`、`sk1/2_世界观人设/casting.md` — 名册行同步
- `tools/gen_shots_sk2.py` — 台词英式用词改美式：`labourer`→`laborer`（S03）、`No lift.`→`No elevator.`（S21 段）
- `5_6_分镜与prompt/` + `4_剧本/` — 全部重生成；残留英式描述核到 0 处

**未改（有意）**：`casting.md` §6 配乐仍走西欧／不列颠语汇。
旧版把「主持人是英国人」也写成了配乐依据之一——那是一条**会随选角漂移的假依据，已删**；
真正的依据是世界观（暴风城是西方奇幻的人类王都，原作配乐即管弦 + 凯尔特底子）。
她换个口音，暴风城不会因此变成一座美国城市。

### Follow-up 007 附记 — c42 酒馆老板补卡完成

闸门查出的缺卡已补：`characters/c42_酒馆老板/`（立绘 1863 字 + turntable 1840 字，一轮出图通过，
读图确认是西欧白人男性、西式中世纪衣着，**没有重演 c27 的东亚脸**）；`c32` 补 turntable 1873 字。
worker 读图纠正了三处只看单图会错的形制（外层是肩带+前襟背心不是马甲 / 袖子同为苔绿是打光造成的色差 /
腰带是很宽的石板蓝带一排骨质半球钉），并判掉了一盏**不属于他的提灯**（同款在另一 NPC 手里是不点亮的通用道具，
且棚拍平光里点一盏灯会和 rule 16.9 打架）。
顺带订正：Vanilla 的「猪和哨声」**是 tavern 不是 inn**，本镜不得出现开房／炉石。
`S09` 参考挂 `c32-1`、`S18` 挂 `c42-1`，`c32` 的 `voice_id` 补锁为 `en-m-sw-baker-01`、字数行刷新。

## Follow-up 009 — 2026-09-19 16:50:00
Source: user_input/follow_ups/202609.md - section 009
Summary: previz 分辨率 720×404 低于 Seedance 的 409600 像素下限，整批重渲 1280×720 并加闸门。

`409600 = 640×640`，是**像素数**下限而不是边长下限。原先渲的 720×404 只有 **290880**，
44 条 previz **全部不可用**——不是 shot1 一条的问题。

Auto-updated:
- `tools/previz_sk2.py` — 默认分辨率 800 → **1280**（输出 1280×720 ＝ 921600，精确 16:9、留足余量）；
  新增 `MIN_PIXELS = 409_600` 闸门：`rx * ry` 不达标直接 `SystemExit`，**渲不出来**而不是渲出一批传不上去的废片
- `5_6_分镜与prompt/shots/*/shot*_previz.mp4` — 44 条全部重渲

**为什么之前没发现**：`--res` 这个参数一直只被当成「渲多快」的旋钮，
没人知道它下游还连着一条**外部模型的硬约束**。这类「产物要满足外部服务的下限」的约束，
和 rule 16.7 ③ 是同一个道理——**必须左移进生成器做成闸门**，不能靠下次上传时再撞一次。

### Follow-up 006 附记 — c27 本尼迪塔斯返工完成

两轮出图通过。worker 挖到两条决定性的一手文字：官方写的是「splendid **white and gold** robes」；
圣光教会「loosely based on **Christianity**」「the hierarchy of **Anglican** ministry」——
**这是「必须是西方教会祭衣」的一手依据**，已正面写进 prompt，而不只是靠负向堵东亚法衣。

读图纠正 9 条，其中 5 条是放大 3–6 倍后才读出来的：袖上金线是**横向圈线**不是竖条纹、
腰间是**倒置盾徽形金属腰甲**不是「雕花腰封·叶瓣浮雕」（「腰封」是东亚词、「叶瓣」直接被画成莲瓣）、
鞋是浅银灰皮鞋配深棕包头不是白色软布鞋（原文纯属臆测）、兜帽贴头而非宽松罩头。
**这再次印证了 `look_from_images.md` §3 的教训：形制细节读不出来就是读不出来，必须放大、必须二源。**

根因补记：上一版是在把 wiki 的 "looks more like a **farmer**" 翻成中文相貌词——
**那句说的是气质，不是长相**，而中文相貌词库自带东亚默认值。这一条已并入 `ai_video.md` §22.1。

轮 1 还暴露了一个次生坑：加了人种锚之后脸对了，但**兜帽被画成尖锥巫师帽**、袖口垂出长尖角布幅——
轮 2 用「圆顶扣住、顶心钝圆，不是尖锥形」「合身直筒袖、腕口平口收住」写死才解决。
**正向写形状比负向列禁忌有效**，与侏儒四指同一条。

Auto-updated:
- `characters/c27_本尼迪塔斯/` — 整卡返工，立绘 1802 字 + turntable 1318 字（无声），重出图通过；废稿留 `c27-1_东亚脸废稿.png`
- `characters/{c24,c32}/ref/refs.md` — 引的还是改名前的 `rNN.jpg`，与盘上的 `refN.jpg` 对不上，已同步（两位 worker 都报了这条）

## Follow-up 010 — 2026-09-19 17:40:00
Source: user_input/follow_ups/202609.md - section 010
Summary: 影像口径从「半写实」改为实拍；look 层做成生成器；27 张场景图全量重出。

**病灶不在图，在 prompt 的第一个词。** 旧的全片共用串是
`渲染样式: 半写实电影质感：形制、比例与配色忠于原作……`，三处硬伤：
① **「半写实」开头**，直接告诉模型别太真，而开头权重最高；
② **「忠于原作」**——原作是一款游戏，正面在要求游戏美术，负面挂着 `游戏引擎渲染`，**正面永远赢负面**；
③ **`材质与做旧:` 主动要求「石面干净、缝口整齐、几乎没有风化」**，那正是游戏资产的样子；
④ 外加 **`瓦色是饱和的中蓝`**——「饱和」本身就是反写实词。
第五条是**位置**：`渲染样式:` 排在第 12 行，前面 11 行已经把「这是个游戏世界」的先验建立完了。

**根因**：把两个互相独立的轴混成了一个。轴一「形制/比例/配色按暴风城设定」该保；
轴二「影像真实度」被一起降成了半写实——而真人实拍一座按暴风城形制搭的实景，照样是写实照片。

**更要紧的是：这条教训三天前就有了。** `ai_video.md` §18 是 sk1 2026-09-16
「太像动画，全部重来」的产物，口径写得很全，但 sk2 没走——所以同一个坑踩了第二次。

Auto-updated:
- `tools/gen_scene_prompts_sk2.py` — **新建**，13 张场景卡 27 张视图的 look 层唯一出处（rule 4i ①）：
  媒介声明（插在内容层之前）/ 摄影的景深与光比 / 做旧通则 / 渲染样式 / 负向成因块；
  带闸门（长度、hex、「半写实」残留）。天空子句**按块条件化**——地下的 bg5-2/bg6、
  室内的 bg10/bg12/bg7-2 不挂天空（rule 16.9）
- `2_世界观人设/scenes/stormwind/*` — 13 张卡、53 处 look 层替换；`饱和的中蓝` 全部去饱和；
  bg2-1 的街灯段指向 `p1` 物件卡（副本，rule 4i ①）
- `tools/gen_shots_sk2.py` — `STYLE_BASE` 换成实拍口径（与场景 look 层逐字同源）；负向从挡症状改挡成因
- `tools/image_fetch.py` — `quality` 只在 `gpt-image-*` 时发送（seedream/gemini 传了 422）
- `.claude/agent_refs/project/ai_video.md` — 新增 §25

**三版迭代的实测结论**：泛泛的「有磨损、有大气」不起作用；起作用的是
**光比写死档数**（受光与阴影差三档、阴影只留很少细节）、**景深点名深度链**
（最近最实、每往深一层软一档、最远只剩偏蓝灰轮廓）、**做旧按材质分条且每条带反例**。
画面从「平、饱和、塑料、全景深」变成「暗部压得住、颜色转灰、石有磨损、远处虚化进雾」。

**诚实结论：仍未到照片级，目前是高级渲染图/概念稿的水平。** 三版走向单调变好，但差距还在。

**模型侧实测**（同一条 prompt）：`gpt-image-2` ＞ `gemini-3-pro-image`（后者出 3D 渲染件、还不服从机位）；
`bytedance-seedream-5-pro`（最可能有照片感的那个）**在本账号被默认禁用**，
报 `model_access_denied`，**需要用户向 ElevenLabs 申请 ByteDance 模型权限**——这是目前最大的一个未测变量。
另记：seedream 的 prompt 硬上限 2000 字符（rule 4d ⑤ 的「硬顶 2000」原来有出处），当前 27 张里有 12 张超。

No conflicts found in: `3_大纲/`, `4_剧本/`, `characters/`, `props/`

### Follow-up 010 执行结果 — 2026-09-19 19:10

27/27 场景图按 v3 实拍口径重出完成（bg4-1 与 bg11-2 首轮超时，单独重试通过）；
44/44 previz 以 1280×720 重渲完成（shot43 渲到第 12 帧时 Blender `EXCEPTION_ACCESS_VIOLATION`
崩掉，`--only S43,S44` 补渲通过——**这是 Blender 侧的偶发崩溃，不是脚本问题**）；
44 个 shot 重生成，参考闸门全过。

**逐张验收的结论：结果按场景类型分化，不是随机的。**

| | 场景 | 判定 |
|---|---|---|
| ✅ 到位 | 旧城区窄巷/门脸/副本门、齿轮洞口、地铁站台、水下两张、大教堂内厅、王座厅穹顶、运河上午、旅店堂屋与壁炉角、矮人区锻造 | 暗部、光轴、脏旧、景深都成立 |
| ❌ 仍游戏味 | 全城航拍两张、英雄谷、城门外仰拍、贸易区两张、教堂广场 | 平光、全景深、缺大气 |

**规律：封闭 / 暗 / 有明确光源的场景全部成功；开阔白天外景与航拍全部失败。**
成因——暗场景**逼**模型按真实光行为走（光必须从某处来、必须衰减、暗部必须黑）；
而明亮大广角**没有任何东西逼它**，它就退回游戏美术的先验（平光、全景深、无大气）。

下一步（尚未做）：对失败的那 7 张**不再用共用串，改按 sk1 的 per-scene 写法**——
逐张点名深度链上的具体物件（「最近的一对巨像最实，第三对开始软，尽头的城门只剩偏蓝灰轮廓」）、
写死光比档数与**逆光/遮蔽**（GoT 日外基本不打光、靠负补光与逆光，见 §18 2.2），
并给航拍补**真实的大气层厚度**。共用串的密度对亮外景不够。

**模型侧已定论**（详见 `ai_video.md` §25）：三个模型同题实测，`gpt-image-2` 最好；
`gemini-3-pro-image` 差；**Seedream 5.0 最差**（风格化游戏美术，队列名 `high_aes_general_v50`
说明它是往美学而非照片调的）。**用户无需申请任何权限**——ElevenLabs 的 ByteDance 通道是
Enterprise-only（本账号 Pro），但本机的 `dreamina` 官方 CLI 早已登录（`vip_level: ultra`）、
可直连 Seedream，实测后判定不值得用。另记：即梦 Seedream 的 prompt 上限是 **1600 字符**，
比 ElevenLabs 那条 2000 更紧。

### Follow-up 010 补正 — 2026-09-19 20:15（用户：「把 bg0-1 改成不要那么游戏味」）

**两处自我更正。**

**更正一：我用缩略图下了判断，判错了。**
上一条里「7 张仍游戏味」是从 480×270 的拼版缩略图得出的。把 bg0-1 放到原尺寸看，
它其实已经有完整的大气消光、清晨低角度长影、炊烟与贴地晨雾——**不是游戏截图**。
这与 `look_from_images.md` §3 记的是同一条教训（形制与影调都不能从缩略图判），**我又犯了一次**。
规矩补一句：**凡对图下结论，必须在原尺寸下看；拼版只用来挑"哪几张需要细看"，不用来定性。**

**更正二：真正的病灶不是画风口径，是一句活下来的旧指令。**
`bg0` 与 `bg1` 的 `材质与做旧` 开头仍写着 **「石面干净、缝口整齐、几乎没有风化」**——
`gen_scene_prompts_sk2.py` 的做旧替换只认了 bg2 那一种措辞（带「只有踩踏面磨出一点包浆」），
bg0/bg1 的变体没被匹配到。于是这两张卡**一边在要求做旧、一边开头就要求干净**，正面互相打架。

**这条解释了之前的分化**：被我判为"仍游戏味"的 7 张里，**4 张出自 bg0 与 bg1 这两张卡**。
不是"亮外景天生难"，是这两张卡带着一句反做旧的指令。（bg2、bg7 的成因另说，待复核。）

Auto-updated:
- `scenes/stormwind/{bg0,bg1}` — 清掉「石面干净、缝口整齐、几乎没有风化」残余（共 3 处）
- `bg0_暴风城全城` — `摄影:` 去掉重复的景深纪律、改写成**直升机实拍航拍板 35mm f5.6 1/500**；
  `材质与做旧:` 加航拍专属的照片线索：**屋顶绝不同色**（补过的新瓦/缺瓦/褪成灰蓝）、
  **云影扫过街区**（被云挡住的暗一档）、炊烟、晾晒衣物、城外不是均匀绿毯；
  并按 rule 16.9 把"街灯罩内无火"这类三百米高空**根本看不见**的反向声明压掉（无效负载）
- `bg0-1 / bg0-2 / bg1-1 / bg1-2` — 四张重出，旧图留 `*_干净城废稿.png`

**验收**：bg0-1 现为真实航拍板（大气分层、远山溶雾、逆光耀斑、屋顶不同色）；
bg1-1 英雄谷为电影板（逆光耀斑、贴地晨雾、湿石板磨损、背光面压得住、远景虚化）。

**教训（已并入 `ai_video.md` §25）**：look 层生成器做**字符串替换**时，
一条指令的**措辞变体**就足以让它活下来，而"正面互相打架"的 prompt 里**先出现的那条赢**。
替换要用正则匹配语义而不是照抄某一张卡的原句，并**在闸门里检残留**（本轮 `半写实` 有检、
`干净/无风化` 没检——已补）。
