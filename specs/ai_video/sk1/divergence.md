# Divergence note — `sk1`（时空旅行 · 汴京清明一日）

本站是**AI 历史情景科普节目**的一站（不是剧情短剧、不是分集剧），系列嵌套于 `ai_videos/shikong_lvxing/`。以下偏离 `agent_refs/project/ai_video.md` 的全局默认，仅作用于本系列，不改动全局；依据均为用户 2026-09-13 的拍板（`specs/ai_video/shikong_lvxing/user_input/follow_ups/202609.md` 002–005）。

| # | 全局默认 | 本站 / 本系列 | 理由 |
|---|---|---|---|
| 1 | 画幅 9:16（rule 7） | **16:9**，不出竖版；竖屏切条是否做留到出片后 | 用户：只做 16:9；同类实测长片是主产品 |
| 2 | 单集 90–120 s（rule 6） | **约 10 分钟，按需可延到 15 分钟** | 用户拍板（系列 follow-up 002 / 009）；穿越 vlog 实测中位 9m44 |
| 3 | `sub_type=novel` 多集 `episodes/` | **每站 `sub_type=short` 扁平结构**，系列嵌套；**站与站完全独立**（无上一站纠错 / 下站预告 / 跨站伏笔） | 用户：每一站都不需要知道其他站 |
| 4 | 单镜 3–30 s 按 beat | **单镜约 20 s（15–30 s），≈ 36–45 镜**；一个地点 + 一个状态 ＝ 一镜，镜内允许切镜；相邻镜按「硬切 + 景别跳档」设计、`jb= / jbcam=` 逐对机检；每站 ≥ 1 个 20–30 s 一镜到底穿街长镜 | 用户：不要琐碎小视频、镜头自然切换、省掉剪辑 |
| 5 | 一个角色一个 voice_id | **只有旅行者发声**：`voice_id_zh` / `voice_id_en` 各锁一个，视频原声一条 + 另一语种译配一条（2026-09-15 起旅行者台词由视频直接出声，见 #20）；当地人零台词，不建 voice_id | 四站双语发布；系列 follow-up 008 |
| 6 | 人脸走选角库 actor_id | **人脸走用户建立的 Seedance entity**（同《荒野生活》） | 用户既定做法 |
| 7 | 六阶段从「立项」起 | **阶段 0 史料调研先于立项**（`0_research/dossier.md` 285 条事实 + 参考图库）——已作为条件触发阶段写进 `ai_videos__全流程编排` / K34 / rule 17 | 用户硬要求「每集大量 research」 |
| 8 | rule 4d ①「锚点图纯文字自由生成、零参考图」 | **锚点 prompt 以下载的历史参考图为形制依据，参考图允许进 prompt 与参考槽**（`refs.md` 登记许可，非 CC0 / CC-BY 只进 prompt 不入画）。4d ① 禁的是拿 blockout / 灰模渲图当出图参考，本系列不违反这一点 | 用户：每个资产先从网上找大量 reference |
| 9 | rule 4g ②「生成构件、不生成建筑」 | **单体建筑走三视图 AI 出图 + image-to-3D 白模**，按俯视排版图 `city_plan` 摆进 `build_{city}.py`；平直重复的街面 / 城墙 / 桥拱仍走脚本 | 用户：参考图 → AI 图 → Blender 建城 → 俯视排版图 |
| 10 | rule 4d ①「不拿灰模渲图当出图参考」 | **一镜到底镜把 previz 灰模 mp4 上传给 Seedance 作运动 / 几何参考**，长相仍由锚点图 + 风格串给；若实测把灰模质感带进画面，退回「previz 只做机位对账」 | 用户：Blender 渲一镜到底视频上传做 reference |
| 11 | 台词以 `正常台词` 为主 | 旅行者**对镜说话 + 画外独白**；**当地人不开口**，只靠动作、神态、手势与她互动，人群只留听不清字句的环境声 | 古代方言无法确定（用户 2026-09-14，系列 follow-up 008）；双语口型成本 |
| 12 | QC：`武打设计` / `特效设计` | N/A（无打斗、无超自然视觉），显式跳过；`奇观镜头` 用于虹桥 / 御街 / 夜市大景 | 题材 |
| 13 | 固定收尾 | 「带你去看一眼」**不是固定件**，sk1 试做一次看 feedback；**由旅行者本人站到今天的同一坐标**，不带古人 | 用户 2026-09-13；2026-09-14 当地人不开口后改为旅行者本人 |
| 14 | 剧情驱动（主角诉求 + 对手 + 冲突） | **外来游客全开放带逛**：默认有权限到处去，无采访、无被拦；逐块覆盖城市面貌 / 吃 / 住（过夜）/ 娱乐 / 穿 / 人们怎么活动 / 当天大事 / 医馆·官府·皇宫·文人聚处；「障碍」只剩货币、时辰这类她自己要应付的事 | 用户 2026-09-14（系列 follow-up 009） |
| 15 | 负向基线含「现代服饰 / 皮靴 / 拉链」等（K15 / K18） | **林问现代装态的镜（S03–S07 换装前、S08 换装镜的现代装段、S36 今日遗址）从负向删去与她锁定装束冲突的词**（现代服饰、皮靴、拉链、工装等），其余当地人仍按宋制负向 | 系列设定：她穿现代装抵达、到客店才换宋装（follow-up 009）；不删会让模型抹掉她的锁定装束 |
| 16 | K34 ⑥ 每个资产 ≥ 8 张历史参考图 | **现代物豁免**：`p3_三件不变物`、系列名册旅行者（`_series/characters/c1–c6`）现代装态无历史参考图；**时间态 / 同地点主体**（`bg14_客店房间夜` / `bg15_园林雅集` / `bg16_汴京全城五更` / `bg17_州桥夜市`）与**建筑单体** `p9_桥尾脚店单元`（长相依据在 `bg1_虹桥/ref/`）不建独立 `ref/`，`refs.md` 只写指向母主体参考图的指针 | 现代物没有历史图可找；时间态与母主体是同一处地方，复制参考图会让两份漂移（rule 4i ①） |
| 17 | rule 4e：世界锚点选「镜头最多、最能代表世界基调」的地面地点；命名约定 `bg0_` 留给平面图、`bg1_` 是世界锚点 | **世界锚点是 `bg0_汴京全城/bg0-1.png`（高空全城航拍）**：只挂一张北宋原本城门局部；`bg1_虹桥` 降为虹桥地点锚点、改挂 bg0-1；编号不改名（bg0 全城主体 W11 已建、目录与路由键在用）；其余主体挂它时只取基调（蓝天、碧水、做旧、颗粒），高空视角与构图不取 | 用户 2026-09-14（sk1 follow-up 003）；与「晴天航拍」基调（follow-up 004）一致 |
| 18 | 调研 ✅ `boat.001`：汴河河水浑黄；style_guide 原定清晨淡青灰天、薄雾 | **全片天要蓝、水要绿**：白天晴天通透蓝天、所有水面碧绿、雾只许贴水一层很淡的晨雾；夜空深蓝；p8–p10 image-to-3D 三视图仍用阴天均匀光（烘进纹理的蓝天与投影会毁白模贴图），只把水改绿。事实注册表不改；口播不说「河水清绿」 | 用户 2026-09-14（sk1 follow-up 004）：「每个图片天空要蓝，水要绿，好像天气好的时候无人机航拍出来的效果」 |
| 19 | rule 4h §D1 / 4g §K：场景 `.blend` 纯几何、零材质零灯光，previz 不携带美术 | **`bianjing.blend` 在布局之上叠一层写实化（look pass，`tools/look_bianjing.py`）**：材质取色自各锚点图（`bg*-1.png` 采样 + 亮度归一）、表面细节用 CC0 Poly Haven PBR；`G_BLOCKS` / 郊外单元盒换成程序化宋式民居实例、p8 / p9 / p10 替身换成同包围盒的细模、柳树球换成垂柳、歇山体块换成曲面屋顶 + 斗拱带、城楼盒换成木构城楼；物理天空 + 卯时偏晚低日光 + Cycles。**布局仍只由 `build_bianjing.py` 决定**：被替换的布局网格保留（hide_render），QC / previz 地面探针 / 几何摘要照旧只看布局层；look 生成的一切带 `look=1`，`undress()` 可整层撤掉。shot previz mp4 仍按 rule 4h 用 workbench 灰模渲（`color_type=OBJECT` 不吃材质），blend 本身可直接用 Cycles 出写实图 | 用户 2026-09-15（sk1 follow-up 005）：场景 blend「不要只是方块，要利用这些图片，越真实越好」；shot blend ＝ 拷贝场景 blend + 人物与动作 |
| 20 | rule 12.4-H：视频不带人声，台词由 TTS 另行合成后 mux | **旅行者的台词由视频模型直接出声**：有台词的镜在 `台词:` 后跟一行 `声音:`（系列卡「声音锁定串」逐字），`参考:` 挂 `{名}声音(cN-2 声样)`；`## 台词配音 prompt` 降为补录（视频语种）与整轨译配（另一语种，先人声分离去原声再 mux）；当地人仍零台词 | 用户 2026-09-15（系列 follow-up 012）：「这次视频里的人是要有声音的，所以 character prompt 要有声音」 |
| 21 | rule 12.5 统一声样台词跨角色 byte-identical（中文一句） | **按旅行者在片里说的语种分两句**：说普通话的候选念 `你好，今天天气还不错，外面很安静。`，说英语的候选念 `Hi, nice weather today — really quiet out here.`；同语种内 byte-identical | 声样要和她在片里说的语种一致，视频模型才抓得准口音与语速（follow-up 012 判断） |
| 22 | 人物卡住在各剧 `characters/`，路由键从 c1 起 | **系列人物只住 `_series/characters/`（旅行者名册 c1–c6）**；本站本地人物键从 `c21` 起（李十六 c21 / 周四娘 c22 / 沈十九 c23）；只属于本站的装束视图挂在系列卡下、键 `c{N}-{站号}{视图}`（sk1 宋装立绘 `-11`、宋装建立视频 `-12`）；衣服形制单列道具卡 `props/p11_旅行者宋装` | 用户 2026-09-15：系列人物「每个 sk1、sk2 里面就不要有了，不然 import 不 work，会有 conflict」；导入器已改为同时向 `_series/` 路由，并对两边同键报 `series_key_conflict`（CLAUDE.md § AI video rules · Series nesting） |
| 23 | 一部剧一位固定主角 | **旅行者可换**：`python tools/gen_shots_sk1.py --traveller cN` 重跑整站（名字、两态锁定串、声音锁定串、视频里说的语种）；镜表里的「林问 / Lin Wen」是占位名，默认 c1 | 用户要从 6 位候选（3 中 3 外）里挑 |

## 平台说明

四站同发（抖音 / 小红书中文，YouTube / TikTok 英文）；只拍一站先看效果，下一站从候选池选（`proposal.md` B 章）。

## 史料纪律（本站）

- 事实注册表 `0_research/parts/w1–w6`；`verified_by` 三态（human / ai_read / ai_draft），`ai_draft` 不进 prompt；用户 2026-09-13 选择先信任 `ai_read`，抽查清单保留在 dossier §0 ③。
- 画面可截图指认的东西只允许 ✅ / ⚠️；❌ 只进「记者踩坑」；⚠️ 镜必须口播「史书没写 / 按……推算」。
- 画面文字只用核对过的真实店名或无字纹样；禁止让模型「写字」。
