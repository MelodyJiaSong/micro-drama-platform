# follow-ups · duikang_shangzeng · 2026-09

---

## 031 — 2026-09-01 12:00:00 — entropy_city 场景文字照 26 张已定稿图全面校正；高架判图错改跨城

> target_stage: 2
> target_artifacts:
>   - 2_世界观人设/scenes/entropy_city/_blender/blender_build.md
>   - 2_世界观人设/scenes/entropy_city/scene_build_flow.md
>   - 2_世界观人设/scenes/entropy_city/bg*/bg*.md
>   - tools/gen_scene_prompts.py
> severity: medium

`_blender/blender_build.md` 的 `参考:` 段已过期（指着 `广场正向.png` 这类盘上不存在的名字，
且只列 5 张锚点），要求它吃透 `scenes/entropy_city/` 下的全部信息 + 已导入的全部参考图，
重写成能直接交给 Claude 去建 Blender 场景的指令；并把由此暴露的所有不一致**全局改一致**。

### 已做

1. **重写 `_blender/blender_build.md`**：整份即指令（新会话读它即可开工）；
   26 张图逐张列真实路径并标注各自证明哪块几何；空间契约、几何事实表、精度分级
   （rule 4g §E）、平面图 + 12 组灰模对照渲染的完整命令与机位坐标表。
2. **文字照图校正**（改 `tools/gen_scene_prompts.py` 的 `SCENE` / `VIEWS` 后重跑，
   26 条 prompt 一次同步）：
   - 广场：五栋独立塔 → **东西两道连续街墙**（东壁 8–12 层、西壁 5–8 层）；
     北开口 14m → **23m**（广场直接收成街廊）；补写南开口 + 下沉引道。
   - 主街：路宽 14m → 车行道 15m + 人行道各 4m ＝ **楼到楼 23m**；商铺 3–6 层 → **4–8 层**、
     开间约 5m；补拱架位置（开口以北 70m）与净空（6.5m）、广告屏间距。
   - 隧道：断面 14×6m → 行车净宽 **10m** + 两侧 0.4m 边石 ＝ 总净宽 **12m**、净高 6m；
     顶灯改**双排**；洞门拱形 + 下沉引道。
   - 全城：肌理高度按图分级（近广场 30–45m、边缘 15–20m），补街区进深与巷宽。
   - `广场斜瞰` 机位标称 80m/45° → **30m/20°**，与已交付的 bg1-4 对齐。
3. **高架：唯一一处判图错而非判字错。** `bg3-1` 出成了跨江大桥（桥下水面、对岸天际线），
   但 `3_大纲/arc_outline.md` 的 shot41「下方的**城市**仍在褪色」与 shot52「从高处俯瞰：
   **整座城市**……复原」要的是桥下是城市，`bg1-1`／`bg5-1` 两张图也把这道桥画在长街上方。
   → `场景:` 改写为**穿城高架**（桥面形制仍照 bg3-1：宽度／护栏／双黄线／路灯／连续梁与墩／
   平缓弯，桥下换成城市）；反向词加「江面、河流、湖面、海、水面、对岸、跨江大桥、
   桥墩立在水里、滨江天际线」；`bg3_高架.md` 顶部挂过期告示。
   **`bg3-1/2/3.png` 三张待重出**，先出正向定稿再出反向与材质。
4. **去重**：`scene_build_flow.md` 里那份「② 正向 / ③④⑤⑥ 派生」prompt 摘要表删掉——
   它与主体 md 是同一份 prompt 的两个副本，已经漂到「参考 ＝ 平面图」的旧路线上。
   现在指向主体 md，并写明改 prompt 要改生成器再重跑。

### 教训（已写进产物）

- **一份 prompt 只应有一个出处。** 摘要表与主体 md 并存必然漂；漂的那份还会被当成真的读。
- **图与文字冲突时先问「谁错」，不要默认听图。** 广场/主街/隧道三处是字错（图对），
  高架一处是图错（剧情对）——判据是**剧情需求 + 其它图的交叉印证**，不是哪个更新。
- **文字尺寸如果是先建模时代凭空定的，它不是证据。** 校正后每条事实都挂了「证据」列。


---

## 032 — 2026-09-01 14:30:00 — 按 blender_build.md 执行：重建 entropy_city 几何

> target_stage: 2
> target_artifacts:
>   - tools/build_entropy_city.py
>   - 2_世界观人设/scenes/entropy_city/_blender/entropy_city.blend
> severity: medium

执行 §031 留下的待办：按 `_blender/blender_build.md` 把 26 张定稿图里的几何落进 blend。

### 做法上的一个决定：写 builder，不手改 blend

blend 被 gitignore（媒体走 R2），**手改过的 blend 没有可审阅的来源**。所以不手动校正旧
blockout，而是写 `tools/build_entropy_city.py` 从零确定性生成——删掉 blend、重跑，得到同一座城。
这跟 §031 里「一份 prompt 只应有一个出处」是同一条原则：**产物的来源必须是可读可改的那一份**。
三个一次性补丁脚本（`add_city_fabric` / `add_tunnel` / `fix_plaza_opening`）随之删除——
它们打在旧 blend 的物件名上，对新 blend 重跑只会打坏。

### 自查与灰模抓出的三个缺陷（文档看不出来，渲了才看见）

1. **旧 blend 根本没有 collection**：324 个物件全在默认 `Collection` 里，
   `BG1_PLAZA` / `BG2_STREET` / … 一个都不存在，只有命名前缀。blender_build.md 却把
   「collection 名不许改」列为不可动项——**它假设了一个并不存在的东西**。已按该命名建出 5 个。
2. **隧道洞口在广场里面**：旧 blend 洞身 Y −20→−170，而广场是 y −30..30 ——
   洞口 Y=−20 落在广场地面上。灰模一渲就看见拱门骑在广场里。已把引道挪到 Y −30→−50、
   洞身 −50→−200。
3. **对照机位的三个坑**（都会让人误判成"几何没建"）：广场正向站 y=−32 会站到广场外面、
   被南出口门洞框住（应 −29）；隧道机位 Z 要跟着洞内路面 −1.2（车头高度 ＝ 0.0 而非 1.2）；
   桥上机位给 10.5 时只比护栏顶高 0.1m，护栏被压成一条贴地平线的细线、看着像没建（应 10.8）。
   三条已写进 blender_build.md §8.2 的机位表下面。

### 教训

- **「文档记载的现状」不是现状。** blender_build.md §6 抄的是 changelog 里的记载（5 塔 / 4 墩 /
  有 collection），实际是 6 塔 / 5 墩 / 零 collection。开工第一件事必须是自查对账，
  这条已经写在 §6 里、这次也确实靠它抓出了问题。
- **渲图是几何体检，平面图和透视各抓各的**。平面图抓「街廊通不通、肌理有没有侵占走廊」，
  灰模透视抓「洞口在不在该在的地方、街廊有没有中途豁然开阔」——后者平面图完全看不出来。

### 追加（同日，用户问「高架桥以及两边的城市有画出来么」）

桥建了，**桥两边的城市基本没有**——一量就露馅：桥两侧 80m 内每 50m 只有 0–4 栋楼，
北侧十二段里九段是零。根因是城市肌理用「到原点距离 > 320m 不生成」的**圆形裁剪**，
而桥在 y≈260、全长 600m，桥的两头早就出了那个圆。

**教训：覆盖范围要按「镜头要拍到什么」定，不要按「到原点多远」定。**
原点是广场，桥不在原点。改成矩形范围（x ±345、y −300→400）后，桥全长每 50m 两侧
都有 1–5 栋楼。同时补了 6 栋 58–80m 的高板楼打断天际线——位置写死不随机，
它们是构图元素（bg1-1 长街尽头那栋、bg5-1 天际线上的几栋）。

顺带发现的第二个坑：为省物件数把远处街区从「2×2 个体量」合成「一个 52m 大体量」，
反而更糟——大体量一旦压到走廊就被整块剔掉，在桥边留出大洞。**小体量塞得进走廊之间，
大体量只会被整块剔掉。** 已改回一律 2×2。

肌理 286 → 436 物件，全场 404 → 554。


---

## 033 — 2026-09-01 16:10:00 — 高架改回跨水弯桥：我把两个东西当成了一座桥

> target_stage: 2
> target_artifacts:
>   - 2_世界观人设/scenes/entropy_city/_blender/blender_build.md
>   - tools/build_entropy_city.py
>   - tools/gen_scene_prompts.py
>   - 3_大纲/arc_outline.md
> severity: high

用户指出：`bg3-1` / `bg3-2` 两张图明明白白是**弯着跨水**的桥、**两岸都是城市**，
问 blender 里有没有体现。**没有** —— §031 我把它判成「跨城」，还把水写进了负面词。

### 我错在哪：这是个伪冲突

我当时看到三条"证据"打架：
- bg3-1 / bg3-2：桥跨水，对岸是城
- arc_outline shot41：「**下方的城市**仍在褪色」
- bg1-1 / bg5-1：长街上方有一道横跨结构

于是判「两张图 vs 一句大纲 + 两张图」，选了跨城。

**真正的问题是第三条根本不是同一个东西。** 长街上方那道横跨结构是一道**窄天桥**，
bg3 那座是城北水面上的跨水弯桥 —— 两座桥，两个地方。把它们当成一座，才逼出了
"图和大纲二选一"。分开之后没有冲突：街上有天桥，城北有跨水弯桥，
shot41 只需把「下方的城市」改成「两岸的城市」。

> **教训：发现冲突时，先问「这两个东西是不是同一个东西」，再问「谁对谁错」。**
> 在伪冲突上做取舍，取出来的结果两边都不对——我既删掉了图里真实存在的水，
> 又把 bg3 三张好图标成了待重出。

### 改了什么

- **几何**：高架从「600m 穿城直桥」改成 **758m 跨水弯桥**——主街往北到头，匝道
  （y 190→300）在街上方爬升到 9.3m，再以 R=900m、转角 41° 的平面弯跨过水面到 (223, 893)；
  18 根桥墩**立在水里**；补水面（y 330→900）、对岸天际线（y 905→1125，28–95m 密集高层）、
  一座平行的拱墩老桥；长街上方另立一道**窄天桥**（y=150）。全场 554 → 768 物件。
- **prompt**：`SCENE["高架"]` 改回跨水（弯道 / 水面 / 桥墩立水里 / 平行老桥 / 两岸城市），
  广场·主街·全城三段里「高架横过长街」的说法改成「天桥」；
  **撤掉 bg3 的过期告示与「江面/水面/对岸」负面词** —— 图没废，也不用重出。
- **叙事**：`arc_outline.md` shot41「下方的城市」→「两岸的城市」；
  幕Ⅳ描述「从高架上看下去」→「从跨水高架上望出去」。（这是本轮唯一改动的叙事文字。）

### previz 回归（§8.4）结果

10/10 通过，全部基于最终 blend（blend 存于 09-01 22:33，十个 mp4 的 mtime 都晚于它）：
shot 01 / 16 / 21 / 22 / 40 / 41 / 46 / 47 / 49 / 52。

**过程中有一次假失败，值得记：** 批量跑时 16/21/22 报错，单独重跑三个全过。
原因是我前后起了三批 previz（前两批用的是还没修完的 blend），中途用 TaskStop 停批——
**TaskStop 杀的是 shell，已经 spawn 出去的 `blender.exe` 还在继续写那三个 shot 的文件**，
最终那批撞上被占用的文件。失败的三个恰好就是被停批当时正在处理的位置。

> **previz 批量任务不能重叠跑。** 停批之后要确认 blender 进程真的退了再起下一批，
> 否则失败信号是假的，会把人引去查几何——而几何是对的。
> 更省事的做法：blend 定稿之后再跑 previz，别边改边跑。

