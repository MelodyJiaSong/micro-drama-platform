# follow-ups · ai_video_management · 2026-09

---

## 160 — 2026-09-05 — previz 脚本改名：认 `shot{NN}_previz.py`

> target_stage: 6
> target_artifacts:
>   - libs/infrastructure/writers/previz__writer.py
> severity: low

### 起因

`ai_video.md` 新增 rule 4h §C：per-shot 的 S 档 previz 编排脚本必须叫 `shot{NN}_previz.py`，
**不得与仓库通用引擎 `tools/previz/build_previz.py` 同名**——同名会让一份合法的 per-shot
脚本在盘上看起来像「引擎被 fork 了一份私货」，也让真正的 fork 无法被一眼认出。
xianjian_yi_mv 的 `shots/shot12/previz/build_previz.py` 据此更名为 `shot12_previz.py`。

### 改了什么

`PrevizWriter._rebuild()` 原本按硬编码文件名 `build_previz.py` 找脚本、并从中解析
`SCENE_MASTER` 常量来重建 blend。抽出 `_build_script(previz_dir)`：
`shot*_previz.py`（glob，取排序首个）优先，`build_previz.py` 作旧名兜底，都没有则返回
`None`、维持「按现状渲染 blend」的旧行为。

### 教训

**改文件名要连工具链一起改。** 这个 writer 是按文件名找脚本的，只认旧名的话，
改名当天 webapp 的「出片」按钮就会**悄悄不再重建 blend**——不报错，只是渲的是上一版几何。
这类「静默降级」比报错危险，因为没人会去看。已把这条写进 `ai_video.md` rule 4h §C。

---

## 161 — 2026-09-06 — YouTube AIGC 系列选题调研模块（research module）

> target_stage: 6
> target_artifacts:
>   - apps/api/routes/
>   - libs/application/queries/
>   - libs/infrastructure/readers/
>   - apps/ui/src/components/
> severity: medium

### 需求

在 `ai_video_management` webapp 里新开一个**调研（research）模块**，承载一份
「近半年 YouTube AIGC 视频系列选题排行」调研结果：**10 大系列**，每个系列下
**10 条 example 视频链接**。

### 两条硬筛选标准（用户口述，优先级最高）

1. **回报/收入好** —— 判据是 **点赞率（like/view）高 + 播放量（view）高**。
   两者都要看，不能只看其一：高播放低赞＝标题党/推荐流意外；高赞低播＝小众。
2. **一个人能翻拍（solo-replicable）** —— 用户是**单人作业**：
   - **工具不是约束**：现有链路是 Claude + Cascadeur + Blender + ElevenLabs + Seedance，
     但缺什么工具「可以很轻松买」，所以不要因为「需要某个付费工具」而淘汰一个系列。
   - **人力是硬约束**：凡是需要真人出镜/实拍/摄制组/配音演员/专业剪辑团队，
     或单集需要数十小时手工建模动画的系列，**直接淘汰**。
   - 判据措辞应写成「单人 + 可购买工具，多少小时/集」，而不是「是否只用现有 5 个工具」。

### 系列（series）的定义

「系列」＝**可复制的内容格式/品类**，不是某一个频道，例如：荒野求生、AI 武侠剧、AI 跳舞。
一个系列下的 10 条 example 必须是真实存在、可打开的 YouTube 链接。

### 数据必须真实、可验证

播放量/点赞数/发布日期一律取自 `yt-dlp` 实测，不得凭印象或搜索摘要估算；
调研窗口 = 近半年（2026-03-06 起）。

### 交付形态

- 数据落盘为结构化文件（可被 webapp 读取、可复跑刷新）。
- webapp 侧新增一个「调研」模块（导航入口 + 页面），可按点赞率/播放量/翻拍难度排序浏览。

---

## 162 — 2026-09-06 — 调研模块要做成「可管理的工作台」，不是 JSON 阅读器

> target_stage: 6
> target_artifacts:
>   - apps/ui/src/components/Research*.tsx
>   - libs/infrastructure/writers/research__writer.py
>   - apps/api/app_factory.py
> severity: medium

### 用户原话（要点）

「don't just give me a json file to read on UI，我要 UI 的原因是你可以给我建一个
feature rich、multilayer 的 UI 来 **view + manage** 内容。」

### 抽象出的规则（对所有数据型模块通用）

1. **把数据落成 JSON 再渲染一张表 ≠ 交付了一个 UI 模块。** 用户要 webapp，是要
   **多层次、可操作**的界面：能概览、能钻取、能筛选、能对比、能留下自己的判断。
   只做「只读渲染」等于把 JSON 换了个字体。
2. **「manage」＝ 用户的决定要能被持久化**，并且**与被管理的数据分文件存放**——
   数据可以随时重新生成，用户的决定不能被生成过程覆盖。
   本模块的落法：`{dataset}.workspace.json` 与 `{dataset}.json` 并排放，前者按
   slug / video_id 引用后者，所以重跑调研后状态/评分/笔记依然对得上。
   （与 `promoted.md` 在 regen 中被 preserve 是同一条原则。）
   推论：数据集列表必须排除 `*.workspace.json`，否则用户的决定会被当成一个数据集列出来。
3. **深链接是功能的一部分，不是可选项。** 一旦视图状态（tab、打开的条目）进了 URL，
   直接打开或刷新就必须能还原；SPA 回落缺失会让「多层次 UI」在刷新那一刻全废。
   `StaticFiles(html=True)` 只对目录路径回落，必须补 catch-all，
   但 `/api/*` 要保持 JSON 404，不能返回 HTML 外壳（否则客户端极难排错）。

### 交付形态

调研模块从「一页卡片列表」改为四视图工作台 + 详情抽屉：
总览（象限散点）/ 系列（筛选+定状态+评分）/ 样本库（100 条可查询表）/ 对比（并排矩阵）。

---

## 163 — 2026-09-06 — 工具箱补 Suno；调研加「收益预估」；导航按月分层

> target_stage: 6
> target_artifacts:
>   - ai_videos/_research/
>   - tools/yt_revenue_model.py
>   - apps/ui/src/components/Research{Nav,Revenue}.tsx
> severity: medium

### 用户三条

1. **工具箱里还有 Suno 和 Cascadeur** —— 之前把 Suno 列成「需新增」是错的。
2. **给链接**。
3. **要视频半年内的预估收益**。
4.（同轮追加）**左侧导航层级**：选题调研 › 调研结果 › 2026-09（按月一个 item）› 10 大分类，
   点分类在 main panel 出详细内容。

### 抽象出的规则

1. **用户的工具箱是输入,不是猜测。** 任何「需新增工具」的判断都必须对齐已声明的工具箱
   （Claude / Cascadeur / Blender / ElevenLabs / Seedance / Suno），并把它写进数据集的
   `owned_tools` 字段,而不是散落在正文里。
2. **收益预估的主导项不是 RPM,是变现门槛。** `播放量 × RPM` 是最容易错一个数量级的算法——
   YPP 未通过前 AdSense 严格等于 $0。任何收益模型必须先算门槛何时通过,再只对门槛之后的
   播放量计费,并显式扣审核期与 $100 起付线。
3. **新手的播放量不能用榜单中位数推。** 数据集里的中位数来自已有几十万订阅的号;
   要回答「我半年能拿多少」,只能实测**近期从零开号**的同赛道频道,再按单人可持续产能归一化。
4. **调研数据按月组织**（`{YYYY-MM}.json`,带 `group` / `month` 字段),
   导航从 API 生成「组 › 月 › 分类」三层;分类是数据集内的 series,不落成文件——
   落成文件就会与数据集产生副本并漂移。

---

## 164 — 2026-09-06 — 详情不再弹窗；半年回报成为唯一排序指标，<$100 一律不列

> target_stage: 6
> target_artifacts:
>   - apps/ui/src/components/ResearchPage.tsx
>   - ai_videos/_research/2026-09.json
>   - tools/yt_revenue_model.py
> severity: high

### 用户两条

1. **UI 不要 pop up** —— 点左侧导航，右侧 main section 变化即可，不要遮罩抽屉。
2. **半年周期回报是最重要的指标**，6 个月回报小于 $100 的视频/系列不要给我。

### 抽象出的规则

1. **导航驱动主区域，不叠浮层。** 从树/导航选中一个条目时，主区域内容替换为该条目详情；
   浮层只用于确实需要打断的操作（确认、破坏性动作），不用于「看内容」。
2. **排序指标一旦改变，候选集必须重新生成，而不是在旧榜单上过滤。**
   旧榜按点赞率选出的 10 个，用回报模型重算后 9 个是 $0——因为「点赞率高」与
   「新号能在 4 个月内过 YPP 门槛」是两个几乎不相关的性质。
   换指标 ＝ 重新做发现，筛选条件要写进 discovery 的提示里，不能只做后置过滤。
3. **订阅转化率是 per-format 的，不是常数。** 实测 49 播放/粉（美国老年福利）到
   730 播放/粉（印地怀旧），差 15 倍；用同一个全局常数会把「能赚钱」算成 $0。
   凡是把播放量换算成订阅/门槛的模型，转化率必须按格式实测取值。
4. **限流与错误 ID 长得一模一样。** 批量抓取失败的条目必须以更小批次、更低并发重试
   两轮后才能判定为「无法解析」，否则会把真实证据当成脏数据丢掉
   （本轮 12 条「失败」里 8 条是限流，重试后全部拿到）。

---

## 165 — 2026-09-09 09:00:00 — series-folder-nesting-for-ai-videos

> target_stage: 6
> target_artifacts:
>   - projects/ai_video_management/libs/common/drama_ref.py
>   - projects/ai_video_management/libs/infrastructure/readers/tree__reader.py
>   - projects/ai_video_management/apps/ui/src/lib/dramas.ts
> severity: high

### 用户指令（抽象后）

一部剧不再一定是 `ai_videos/` 下的一层目录：**多部同系列的短片需要一个共同的顶层目录**
（`ai_videos/{series}/{episode}/`），磁盘与 UI 都要如此。用户在被告知重构代价后仍选择真嵌套。

### 抽象出的规则

1. **「剧集身份 ＝ 单层目录名」这个假设必须集中到一处，不能散布在路径解析里。**
   本次之前它被硬编码在 `DramaPath`（只接受 2 段）与 15 个文件 / 20 处 `parts[1]` 判断里，
   任何层级变化都要逐处改。现在由 `libs/common/drama_ref.py` 统一回答
   「这条路径的剧根占前几段」，其余模块只问它、不自己数。
2. **判定一个目录是不是系列，用显式标记文件，不用命名约定、也不用清单。**
   `ai_videos/{series}/series.json` 存在即为系列；成员剧仍从文件系统派生，
   不在 `series.json` 里维护集数清单（对齐 CLAUDE.md § State surfaces 的
   「状态从文件系统派生」）。
3. **前端不得从路径深度反推剧根。** 后端在树节点上给出 `is_drama` 标记，
   侧边栏、选角、资产提取一律按标记走；扁平剧 2 段、系列成员 3 段，深度不再是判据。
4. **系统库（`_actors` / `_bgm` / `_performances`）不是剧，但仍是导入目标。**
   两类判定要分开写：`drama_depth()` 只认剧；`_validate_drama` 额外放行
   `ai_videos/_*` 两段路径，否则 DownloadsImporter 的表演库导入会被误拒
   （本次回归实测命中 3 个测试）。

### 一句话摘要

把「剧 ＝ ai_videos 下单层目录」重构为「剧根可为 2 段或 3 段」，由 `drama_ref` 单点判定，
后端向 UI 显式标记 `is_drama` / `series` 节点；`荒野生活` 系列（hy1/hy2）落在新结构上。

---

## 166 — 2026-09-12 12:00:00 — cross-episode-asset-shortcuts

> target_stage: 6
> target_artifacts:
>   - projects/ai_video_management/libs/common/asset_link.py
>   - projects/ai_video_management/libs/infrastructure/readers/tree__reader.py
>   - projects/ai_video_management/apps/ui/src/components/Sidebar.tsx
> severity: medium

### 用户指令（抽象后）

同系列后一部沿用前一部的人物 / 道具时，**要能在本片目录下看到一个快捷链接，并且能预览**。

### 抽象出的规则

1. **「同一份资产出现在两个地方」在这个仓库里只有一种合法实现：清单文件。**
   复制字节违反单一出处；OS 软链被每个 reader 的遍历加固主动跳过，在 UI 里等于不存在。
2. **链接节点要把 `path` 指向目标，而不是指向清单。**
   这样 `/api/media`、预览、下载**全部零改动**继承既有能力；
   新增的只有一个 `is_link` 标记，供 UI 画图标。**能力靠复用既有路径拿到，不要新开一条媒体通道。**
3. **解析失败要退回显示裸文件，不要静默吞掉。**
   target 越界 / 不存在 / 是软链 → 不渲染成链接，而是把那个 `.link.json` 当普通 json 显示，
   让用户看得见、改得了。看不见的坏链接比坏链接本身更糟。

### 一句话摘要

新增 `libs/common/asset_link.py` + tree_reader 的 `_link_leaf`，把 `*.link.json` 渲成
坐在本片目录、`path` 指向目标的叶子；前端加 `is_link` / `link_at` / `link_note` 与一个 🔗 图标。

---

## 167 — 2026-09-13 14:10:00 — 人物视频抽帧报 extraction failed

> target_stage: 6
> target_artifacts:
>   - projects/ai_video_management/libs/infrastructure/writers/character_video__writer.py
>   - projects/ai_video_management/tests/test_character_video_path_guard.py
> severity: high

### 指令

在 hy3 里点「从人物视频抽图」报 **extraction failed**，要修。

### 根因

`character_video__writer.py` 的 `_is_under_character_folder` 被标成 `@staticmethod`，
**函数体里却引用了 `self._resolver.root`** —— 每次调用都抛
`NameError: name 'self' is not defined`，在 UI 上表现为 extraction failed。

这是 `8907992`（2026-09-09 系列化重构）引入的回归：那次把扁平的
`parts[0] != "ai_videos"` 判断换成 `drama_ref.drama_depth(root, parts)`（正确方向，
因为剧根现在是两段或三段），但**没有把这个静态方法改成能拿到 root 的形式**。
`CharacterViewExtractor` 那一侧更明显——它是以
`CharacterVideoTruncator._is_under_character_folder(rel)` **无绑定静态调用**的，
连 `self` 都不存在。

**影响面不止 hy3**：truncate / extract-views / extract-all 三个端点对**所有剧**全挂，
只是这些按钮自那次重构后没人点过。

### 修法

提成模块级函数 `is_under_character_folder(root, rel)`（root 显式传入），两个调用点都改；
顺手收紧 `characters/` 的位置判断——只接受 **剧根深度** 或 **剧根深度 + 1**
（legacy 布局 / 阶段目录布局），不再接受埋在更深处、只是恰好叫 `characters` 的目录。
新增 `tests/test_character_video_path_guard.py`（11 例：两种剧深度 × 两种布局 + 7 条负例）。

### 一行摘要

修 `@staticmethod` 里引用 `self` 的回归（系列化重构遗留），人物视频抽帧恢复；加回归测试钉死。

---

## 168 — 2026-09-13 17:30:00 — 同一主体的多视图导入时互相覆盖

> target_stage: 6
> target_artifacts:
>   - projects/ai_video_management/libs/infrastructure/writers/downloads__writer.py
>   - projects/ai_video_management/libs/infrastructure/writers/media__writer.py
> severity: high

### 指令

导入参考图时，同一主体的两个视图（`p3-1` 与 `p3-2`）都被命名成主体目录名
`p3_抹泥板与黏土壁炉.png`，后导入的那张**静默覆盖**前一张，图直接丢失。
修导入功能，并把该剧已被折叠成目录名的资产文件改回路由键命名。

### 诊断

出图工具按 prompt 首行命名下载，并在前后裹上自己的前缀与时间戳：

```
prompt 首行 : p3-2_黏土壁炉锚点
落盘         : ElevenLabs_image_gpt-image-2_p3-2_黏土壁炉锚点 一座手_2026-09-13T07_26_16.png
```

两处都只认「stem **以**目录 token **开头**」：

1. `DownloadsImporter._view_key` —— 开头判定失败 → fallthrough 到 `dst_folder.name`，
   两个视图同名、`shutil.move` 覆盖。
2. `MediaRenamer._plan_folder` —— 更糟：把这类文件折叠成**顺序相关**的 `{folder}{N}`，
   同一张图在不同次导入里编号会变。

场景主体（`bg{N}-{M}`）早就在 stem 里**任意位置**搜键，所以从没犯过；道具与人物没有。
根因是同一套路由键逻辑有两份实现且只改了一份（违反 rule 4i ①「一份东西只有一个出处」）。

### 落地

- 新建 `libs/common/asset_key.py` —— 路由键逻辑的**唯一出处**：
  `folder_key()` 从目录名取该目录**自己拥有**的前缀（`p3` / `c1` / `bg10`；
  `bg1_朝北_城门` 这类单图 plate 返回 None，它的目录名就是键），
  `view_key_in()` 在 stem 里**任意位置**搜 `{该前缀}-{数字}`、前面要求非字母数字边界
  （所以 `gpt-image-2` 不会被误认成键）。**期望前缀只从目录名取，绝不从文件名猜** ——
  这让误报在结构上不可能。
- 两端都改用它：`downloads__writer.py` 命名分支（`prop`/`scene`/`character`）、
  `media__writer.py` 的 `_plan_folder` normalise 通道。
- `_clear_named_media` 的分支扩到 `("prop", "scene", "scene_subject", "character")`。
- `_iter_downloads` 改按 `(mtime, name)` 排序 —— 同一键的两次 re-roll，**最新的 take 胜**
  而不是按目录遍历顺序随机胜。
- 新增 `tests/test_asset_routing_key_collision.py`（5 例）。

### 一行摘要

路由键在两处各有一份实现、只有场景那份支持「键在 stem 中间」，导致道具/人物多视图导入互相覆盖；
抽成 `libs/common/asset_key.py` 单一出处、两端共用，并加回归测试。

---

## 169 — 2026-09-17 23:20:00 — UI 加 .glb 3D 白模预览

> target_stage: 6
> target_artifacts:
>   - projects/ai_video_management/libs/infrastructure/readers/tree__reader.py
>   - projects/ai_video_management/apps/ui/src/components/SiblingMedia.tsx
>   - projects/ai_video_management/apps/ui/src/components/Sidebar.tsx
>   - projects/ai_video_management/apps/ui/src/lib/linkResolver.ts
> severity: medium

### 指令

`ai_videos/` 下开始产出 `.glb` 三维白模（sk1 的 `props/p{N}_*/whitemodel/raw.glb` 等），
要能在 webapp 里预览。

### 现状盘点（先查再改）

单文件预览**早已存在**：`Reader` 有 `MODEL_EXTS` 分支渲 `<model-viewer>`，
`/api/media` 发 `model/gltf-binary` + `inline`，`@google/model-viewer` 已在依赖里。
缺的是**看得见**——

1. 白模落在 `whitemodel/` 子目录，而 `SiblingMedia` 只看**同 folder** + `renders/` + `archive/`，
   所以主体卡片（`p19_独轮串车.md`）上一个字都不提它；
2. `SiblingMedia` 的 `MEDIA_EXTS` 不含 `.glb`，即使网格就躺在同 folder（`f80_ferrari/base.glb`）也不显示；
3. 树叶子类型是兜底的 `file`，侧栏没有图标区分，扫一眼看不出哪个是网格。

### 落地

- `tree__reader.py` 加 `_MODEL_EXTENSIONS` → 叶子类型 **`model`**（原为 `file`）。
- `Sidebar.tsx`：`model` 记为可点叶子 + 🧊 图标。**叶子类型判定原先复制了 5 份**，
  先收敛成 `FILE_LEAF_TYPES` / `LEAF_TYPES` 两个集合再加类型。
- `linkResolver.collectFilePaths`：第 6 份拷贝，同样加 `model`。
  **这一处是改类型引出的真回归**——`knownPaths` 是所有 sibling-media / 链接解析的过滤底表，
  漏了它，`.glb` 反而从"能显示"退成"完全不可见"（浏览器实测抓到）。
- `SiblingMedia.tsx`：`.glb`/`.gltf` 进 `MEDIA_EXTS`（tile 渲 `<model-viewer loading="lazy">`），
  新增 `findSubfolderModels` + **🧊 3D 白模**一节，把下一层子目录的网格提到卡片上；
  只捞网格不捞图，且跳过 `renders/`/`archive/`（那两节已各自覆盖），一个网格只出现在一个 tile 里。
- CSS：`model-viewer` 无固有尺寸，必须显式给宽高否则塌成 0 高渲成空白；
  底色改深（白模材质被剥光，浅底上是白对白）。

### 验证

- 后端：`tests/test_media_glb_preview.py` +2 例（叶子类型 = `model`；`whitemodel/` 子目录里
  `.glb` 进树、`.blend` 不进）。全量 337 passed / 24 failed，24 例为**预先存在**
  （引用已不存在的 `wukong_juexing` 等），改前改后同一份。
- 前端：`apps/ui/test/siblingModels.test.ts` 新增 5 例（含 `collectFilePaths` 保留 `model` 的回归钉）。
- 端到端（真浏览器）：sk1 `p19_独轮串车.md` 卡片出 🧊 一节、独轮车白模可拖动旋转；
  `whitemodel/raw.glb` 单页全幅可转；`f80_ferrari.md` 同 folder 4 个 `.glb` 各自成 tile；
  侧栏 `raw.glb` 显示 🧊 且可点。

### 一行摘要

`.glb` 白模在 UI 里可见可转：树叶子独立类型 + 侧栏 🧊 + 卡片新增「3D 白模」节（捞子目录网格）+ sibling tile 渲 model-viewer；
顺手收敛了散在 6 处的叶子类型判定（其中 1 处漏改会让 `.glb` 从树里整个消失）。

---

## 170 — 2026-09-19 16:10:00 — /api/tree 慢到 vite 代理报错，侧栏起不来

> target_stage: 6
> target_artifacts:
>   - projects/ai_video_management/libs/infrastructure/readers/tree__reader.py
> severity: medium

### 指令

前端起不来：vite 反复报 `http proxy error: /api/tree — connect ECONNREFUSED 127.0.0.1:8766`。
修掉它。

### 一行摘要

报错文本是 ECONNREFUSED（后端还没 bind 完就被打），但**真正的病是 `/api/tree` 要跑 42 秒**——
20,573 个节点、2.9 MB，前端等不到就重试，重试又叠上一轮全盘遍历。
实测归因：`_rel()` 占 build 的 68%（每个节点做一次 `Path.resolve()`，42,034 次
`nt._getfinalpathname` ＝ 9.5s；再加 `Path.relative_to()` 8.3s），而 root 早已 resolve 过、
walker 又显式跳过 symlink，这两步**结果恒等于字符串切片**；其次是 `iterdir()` + 逐个
`is_dir/is_file/is_symlink` 打出的 106,926 次 `nt.stat`（5.1s），`os.scandir()` 的
DirEntry 在 Windows 上本来就带着这些位。判据是 profile 数字，不是猜测：
`jsonable_encoder` 0.35s、`json.dumps` 0.15s、pydantic 校验 0.00s，序列化层是干净的。

---

## 171 — 2026-09-19 23:10:00 — 生成末帧按钮在 mp4 页面上找不到

> target_stage: 6
> target_artifacts:
>   - projects/ai_video_management/apps/ui/src/components/Reader.tsx
> severity: low

### 指令

「从 mp4 生成末帧、并自动把本镜末帧复制成下一镜首帧」的按钮，在打开 mp4 时看不到；
把它放到 mp4 自己的预览页上。

### 一行摘要

功能没丢——`⏮ 生成末帧` 一直在 `SiblingMedia` 的媒体卡上（要先打开 `shotNN.md` 再往下翻），
后端 `/api/extract-last-frame` 与 `FrameExtractor.extract_last_frame` 也完好。
真正的缺口是 **Reader 的 mp4 单文件视图漏了这一个按钮**：同一行里另外三个抽帧按钮
（🎞 Extract Frames / 🖼 提取三视图 / 🧭 截取方向背景图）都在，唯独 shot 视频的末帧按钮没有，
于是它看起来像是被删了。按同样的形状补上 `showLastFrameBtn`（gate = `isShotVideoPath`）。
