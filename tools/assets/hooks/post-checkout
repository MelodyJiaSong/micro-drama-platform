#!/bin/sh
# assets_sync: switching branches can swap in a different index; fetch whatever
# media the new one names. $3=1 means a branch checkout, not a file checkout.
[ "$ASSETS_SYNC_AUTO_PULL" = "0" ] && exit 0
[ "$3" = "1" ] || exit 0
"@PYTHON@" tools/assets_sync.py pull --auto
exit 0
