#!/bin/sh
# assets_sync: a pull brings the index; this brings the bytes it names.
# Skip with ASSETS_SYNC_AUTO_PULL=0 (then run `assets_sync.py pull` by hand).
[ "$ASSETS_SYNC_AUTO_PULL" = "0" ] && exit 0
"@PYTHON@" tools/assets_sync.py pull --auto
exit 0
