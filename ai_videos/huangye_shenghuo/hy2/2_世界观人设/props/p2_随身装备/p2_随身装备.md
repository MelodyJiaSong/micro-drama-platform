# 随身装备 · p2

> **跨片同一套。** 锁定描述符与 hy1 一致。
> **本片不出 ref 图**——`p2_随身装备.png` 已通过快捷链接挂在本目录下（UI 里可直接预览），
> 指向 `hy1/2_世界观人设/props/p2_随身装备/p2_随身装备.png`。

## 锁定描述符（跨片 byte-identical）

```
深灰色旧登山背包，外挂系带磨损；磨砂铝制水壶；米白编织绳一卷；黑柄镁棒打火石；熏黑单柄铝锅
```

## 背包状态（`ai_video.md` rule 16.2 · 生成器 `bag` 字段强制）

| 镜 | 状态 |
|---|---|
| `shot01` · `shot02` | **背在身上**——他还在找地方 |
| `shot03` 的 2–5s | **镜内卸下**，放在根盘底部的干处 |
| `shot04`–`shot12` | **一律不在身上**，身上只有腰侧刀鞘与腰间水壶 |

**位置跨镜固定在根盘底部的干处**——这是无声叙事里「这是同一个家」的锚点之一。

## 本片用到的两件

- **镁棒打火石** — `shot10` 生火。
- **铝锅** — `shot08` 归置时放在床尾的石板上；`shot12` 仍在原位，不给特写、不解释。

## 负向（写进用到它的 shot 的负向块）

```
崭新装备, 品牌 logo, 鲜艳配色, 现代战术背包, 塑料扣具反光, 干活时背着背包
```
