# shot06 previz —— **雨滴悬停在半空**，一动不动（失速铁律的最强单点）
# 成片 1.1s ／ 生成 5.0s ／ 入点 0:11.1 ／ bg2 主街 ／ 85微距 ／ 特写·固定
# 档位 C：时刻表档——单主体 proxy + 动作关键帧，不建环境；previz 只解「这个动作分几拍、每拍几秒」
#
# 坐标：+X 画右，-X 画左，+Y 画深处，Z 高度。朝向：0 面朝镜头，90 面朝画右，-90 面朝画左。
# ⚠ 本文件是【骨架】：方位角、道具坐标、关键帧时刻均需按镜手调（标了 TODO 的三类值）。
# ⚠ 关键帧的 t 必须与 shotNN.md 的 `动作:` 时间轴逐拍对齐（follow-up 001）。
# 重跑：blender -b --factory-startup --python tools/previz/build_previz.py -- <本文件>

["全局"]
shot = "shot06"
"项目" = "duikang_shangzeng"
fps = 25
total_sec = 5.0
"场景" = "entropy_city"
"地面" = false
"分辨率" = [2560, 1090]

["机位"]
"焦距" = 85.0
"俯角" = 0.0
"基准主体" = "subject"
"占画高" = 0.85
"横向偏移" = 0.0
"距离倍数" = 1.0
"方位角" = 25.0        # TODO 按镜调：0=正前 90=正侧 180=正后

# 无 [["运镜"]]：本镜机位固定

[["道具"]]
"名" = "subject"
"形" = "box"
"尺寸" = [0.4, 0.4, 0.4]   # TODO 换成本镜主体的真实包围盒
"色" = "白"
"位置" = [0.0, 0.0, 0.0]
  # 时刻表：本镜动作分几拍、每拍几秒。t 必须与 shotNN.md `动作:` 一致。
  [["道具"."关键帧"]]
  t = 0.0
  "位置" = [0.0, 0.0, 0.0]
  [["道具"."关键帧"]]
  t = 5.0
  "位置" = [0.0, 0.0, 0.0]   # TODO 按动作拍点补中间关键帧

