# shot49 previz —— **H · 合一** 马在空中散成粒子，粒子流向车身**左侧翼子板**汇聚。全片唯一一次马的瓦解，且是**向上的、主动的
# 成片 4.2s ／ 生成 12.0s ／ 入点 1:16.5 ／ bg3 高架 ／ 35 ／ 中景·跟随
# 档位 A：完整建场档——车/马/环境体块 + 机位动画；previz 解轨迹、遮挡、尺度与时刻表
#
# 坐标：+X 画右，-X 画左，+Y 画深处，Z 高度。朝向：0 面朝镜头，90 面朝画右，-90 面朝画左。
# ⚠ 本文件是【骨架】：方位角、道具坐标、关键帧时刻均需按镜手调（标了 TODO 的三类值）。
# ⚠ 关键帧的 t 必须与 shotNN.md 的 `动作:` 时间轴逐拍对齐（follow-up 001）。
# 重跑：blender -b --factory-startup --python tools/previz/build_previz.py -- <本文件>

["全局"]
shot = "shot49"
"项目" = "duikang_shangzeng"
fps = 25
total_sec = 12.0
"场景" = "entropy_city"
"地面" = false
"分辨率" = [2560, 1090]

["机位"]
"焦距" = 35.0
"俯角" = 0.0
"基准主体" = "f80"
"占画高" = 0.55
"横向偏移" = 0.0
"距离倍数" = 1.0
"方位角" = 25.0        # TODO 按镜调：0=正前 90=正侧 180=正后
"锁定主体" = "f80"    # 跟随/并驾镜：机位锁主体，不打 rig 运镜

# 无 [["运镜"]]：跟随镜——机位锁主体，靠主体关键帧走

[["道具"]]
"名" = "f80"
"形" = "模型"
"档" = "ai_videos/duikang_shangzeng/2_世界观人设/props/f80_ferrari/f80_ferrari.blend"
"尺寸" = [2.06, 4.84, 1.14]   # 宽×长×高，车头朝 +Y，勿改
"色" = "红"
"位置" = [0.0, 0.0, 0.0]
"朝向" = 0.0
  # 跟随镜：车＝画框锚点，previz 里【保持原点不动】。
  # 机位与车同速平移时，车在画面里的位置不变——真正要 previz 定的是
  # 「其它主体相对车怎么动」，那部分写在马/角色的关键帧上。
  # 向前的绝对位移交给 Seedance（路面流动、环境掠过）。
  [["道具"."关键帧"]]
  t = 0.0
  "位置" = [0.0, 0.0, 0.0]
  [["道具"."关键帧"]]
  t = 12.0
  "位置" = [0.0, 0.0, 0.0]   # TODO 若本镜车相对机位有进退，改这里

[["道具"]]
"名" = "horse"
"形" = "box"
"尺寸" = [0.8, 2.4, 1.75]   # 宽×体长×肩高，马头朝 +Y
"色" = "深灰"
"位置" = [-4.0, 0.0, 0.0]   # 铁律：马恒在画面左（-X）、不超越车（world.md §五）
"朝向" = 0.0
  [["道具"."关键帧"]]
  t = 0.0
  "位置" = [-4.0, 0.0, 0.0]
  [["道具"."关键帧"]]
  t = 12.0
  "位置" = [-4.0, 40.0, 0.0]   # TODO 与车同速：并驾不超越、Y 不得大于车

