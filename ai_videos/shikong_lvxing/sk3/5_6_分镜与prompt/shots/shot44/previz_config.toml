# shot44 previz · 起火时刻 —— 两份一手史料对不上
# 由 tools/gen_previz_sk3.py 从 tools/sk3_data/shots_*.toml 生成——**改镜表重跑，别手改本文件**
# 占画高 ＝ 该镜 jb 起幅值（与 gen_shots_sk3.py 的景别档闸门同一个数）
# ⚠️ 关键帧 t 必须与 shot44.md 的 `动作:` 时间轴逐拍对齐（rule 4h ②）
# ⚠️ **本文件是骨架**：`占画高` 与时长由镜表算准，但**方位角、道具坐标、关键帧时刻仍需按镜手调**。走廊型地点（桥 / 布丁巷）的方位角已按 bg 钉死在沿轴，其余仍是按机位标签关键词猜的。

["全局"]
shot = "shot44"
"项目" = "shikong_lvxing/sk3"
"场景" = "london"
fps = 6
total_sec = 26.0
"地面" = false
"分辨率" = [640, 360]

["机位"]
"焦距" = 28.0
"俯角" = 32.0
"方位角" = 30.0
"基准主体" = "nia"
"占画高" = 0.080
"横向偏移" = 0.0

[["角色"]]
"名" = "nia"
"色" = "绿"
"身高" = 1.72
  [["角色"."关键帧"]]
  t = 0.0
  "位置" = [0.00, -120.00, 90.00]
  [["角色"."关键帧"]]
  t = 26.0
  "位置" = [0.00, -120.00, 90.00]

[["运镜"]]
"类型" = "升降"
"起" = 0.0
"止" = 26.0
"量" = 120.0
