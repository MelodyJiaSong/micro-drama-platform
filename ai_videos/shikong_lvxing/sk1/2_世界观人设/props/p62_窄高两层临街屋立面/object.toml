# p62_窄高两层临街屋立面 白模规格 + 验收清单（`tools/whitemodel_normalize.py` 的输入）。
#
# 写一次，之后**每一次重生成**都过同一套验收——换 Rodin、换 Hunyuan3D、换买来的模型，
# 验收标准不变（vendor 无关，闸门才是质量的出处）。
# 区域坐标一律归一化到包围盒 0..1。

["对象"]
"名称" = "p62_窄高两层临街屋立面"
"中文名" = "窄高两层临街屋立面"

["几何"]
"尺寸" = [3.6, 6, 6.4]        # X宽 × Y长 × Z高，米
"容差" = 0.08
"最多松散块" = 60000    # 生成网格每条缝都是独立壳，几万块是常态；这条抓的是「薄件被甩成碎渣」

# image-to-3D 的输出朝向是随机的；**拿到网格先渲一眼再改这里**（rule 4h §G）。
"源朝向" = { "前" = "-Y", "上" = "+Z" }   # Rodin GLB 常见值，实测后按需改
"目标朝向" = { "前" = "+Y", "上" = "+Z" }

# ---- 占位探针：只判定「这个部件在不在」，判定不了表面质量与拓扑。
# 阈值先给下限占位值；首个被人眼接受的网格落地后，按那一版实测收紧，那一版即回归基线。

[["验收"]]
"名称" = "下层板门"
"类型" = "占位"
"区域" = { z = [0.00, 1.00] }   # ⚠ 待收紧：按该部件在包围盒里的实际位置改
"最少顶点" = 3
"最少面积占比" = 0.004

[["验收"]]
"名称" = "二层栏杆平座"
"类型" = "占位"
"区域" = { z = [0.00, 1.00] }   # ⚠ 待收紧：按该部件在包围盒里的实际位置改
"最少顶点" = 3
"最少面积占比" = 0.004

[["验收"]]
"名称" = "支摘窗"
"类型" = "占位"
"区域" = { z = [0.00, 1.00] }   # ⚠ 待收紧：按该部件在包围盒里的实际位置改
"最少顶点" = 3
"最少面积占比" = 0.004

[["验收"]]
"名称" = "悬山瓦顶"
"类型" = "占位"
"区域" = { z = [0.00, 1.00] }   # ⚠ 待收紧：按该部件在包围盒里的实际位置改
"最少顶点" = 3
"最少面积占比" = 0.004
