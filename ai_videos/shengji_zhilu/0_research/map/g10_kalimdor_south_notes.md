# G10 · 卡利姆多南部 —— 备注（非树数据）

## 0. 自查对账

**本片区由 parent 补写，不是 agent 测绘的产物。** 两轮 agent 均失败（首轮工作流中断、补跑轮 agent error），
而它挡着主树上 4 个孤儿 parent（`tanaris` / `feralas` / `silithus` / `wyrmbog`），
不补就 `build_world_tree.py` 出不了树。

**处置**：按本剧**已核验**的 `parts/w06_全升级路线地图.md`（120 条事实，已过 W06-V 核验）逐条**结构转写**——
w06 的原始 `quote` 与 `source_url` 留在该 part 里，本片区**不冒充原文页**，
全部 23 个节点一律 `verified_by: ai_draft`。

**这意味着**：本片区的节点 **不得直接进 prompt**（stage0 铁律）。
推进到该片区（第一季之后）时，按 G01–G13 的标准重测一次，届时升到 `ai_read`。

**覆盖强度**：只到 **zone + 关键聚居点 + 副本入口所在子区域**，
远低于 G02（暴风王国核心四区，133 节点）那种建筑级精度。**这是有意的**——
本片区不在第一季拍摄范围内，现在做细的收益为零（rule 4f「用到才建」）。

| zone | 节点数 | 够不够用 |
|---|---|---|
| 尘泥沼泽 | 4 | 够——`wyrmbog` 已补上，奥妮克希亚巢穴不再是孤儿 |
| 千针石林 | 4 | 够 |
| 费拉斯 | 5 | 够——`feathermoon_stronghold` / `forgotten_coast` / `camp_mojache` 已补 |
| 塔纳利斯 | 4 | 够——`gadgetzan` 已补 |
| 安戈洛环形山 | 3 | 够——`marshals_refuge` 已补 |
| 希利苏斯 | 3 | 够——`cenarion_hold` 已补 |

## 1. 版本差异（4.0.3a 之后才有 / 才改，**不进树**）

| 地名 / 事实 | 旧世的样子 | 负向词 |
|---|---|---|
| **塞拉摩废墟** | Vanilla 是**完好的联盟要塞**，吉安娜在 | `塞拉摩废墟, 魔法炸弹弹坑, 紫色弹坑` |
| **闪光平原被水淹** | 旧世是**干涸的白色盐碱滩**，上面跑赛车 | `被淹没的闪光平原, 内海, 水面赛道` |
| **羽月渡轮** | 旧世有；`Removed in patch 4.0.3a` | —（旧世**要有**，别漏） |
| **时光之穴可进** | Vanilla **不可进**，只是地标 | `时光之穴内部, 青铜龙副本` |

## 2. 存疑（⚠️）

- **「费拉斯」vs「菲拉斯」**：本剧考据里两种写法并存，W06-V 已裁定为「同一处的两个自译名」，
  但**官方词表本轮没拿到**（灰机 403）。本片区暂用 `费拉斯`（w06 §2／§3.2 的写法），
  待 `pending_user.md` #5 拿到官方词表后作一次独立迁移。
- **尘泥沼泽等级带 35-40 还是 35-45**：`Classic_zones` 与区页 infobox 不符（w06 U3）。
  本片区取 **35-45**，并遵 w06 的总则「一律以 `Classic_zones` 为准」——⚠️ 两者矛盾，推进到该区时复核。
- **「失踪的外交官」终点是否在塞拉摩**：w06 U19 明确「本轮无出处」。**不写进 `note_zh` 当事实**。

## 3. 地图图片（只记 URL 与版权状态，不下载）

本片区未建参考图库（不在第一季范围）。推进到该区时按 `ai_video.md` rule 4d「图先行」补，
并遵 `divergence #8`：**暴雪美术资产只进人眼、不入画、不上传给生成模型**。

## 4. 给下游的提醒

1. **本片区全是 `ai_draft`，一个字都不能直接进 prompt。** 要用先重测。
2. 副本节点（祖尔法拉克 / 厄运之槌三翼 / 奥妮克希亚巢穴 / 安其拉废墟与神殿）**归 `g11_instances.yaml`**，
   本片区只保证它们的 parent 子区域存在。
3. 本剧 concept **D12 裁定「20 级去一趟卡利姆多就回」**——只拍船 + 港 + 副本，
   **不展开灰谷/南卡利姆多的开阔地图**。所以这 23 个节点里，第一季真正会用到的是 **0 个**。
